<?xml version="1.0" encoding="utf-8"?>
<rss version="2.0" xmlns:dc="http://purl.org/dc/elements/1.1/" xmlns:content="http://purl.org/rss/1.0/modules/content/">
    <channel>
        <title>Nano Collective</title>
        <link>https://nanocollective.org</link>
        <description>Updates, discussions, and announcements from the Nano Collective community. Privacy-first open source AI development.</description>
        <lastBuildDate>Sun, 05 Jul 2026 20:27:44 GMT</lastBuildDate>
        <docs>https://validator.w3.org/feed/docs/rss2.html</docs>
        <generator>https://github.com/jpmonette/feed</generator>
        <language>en</language>
        <image>
            <title>Nano Collective</title>
            <url>https://nanocollective.org/logo.png</url>
            <link>https://nanocollective.org</link>
        </image>
        <copyright>All rights reserved 2026, Nano Collective</copyright>
        <item>
            <title><![CDATA[Nanocoder v1.28.0 - ACP for Zed, slimmer tool surface, session resume, copy command]]></title>
            <link>https://nanocollective.org/blog/nanocoder-v1280-acp-for-zed-slimmer-tool-surface-session-resume-copy-command-66</link>
            <guid isPermaLink="false">https://nanocollective.org/blog/nanocoder-v1280-acp-for-zed-slimmer-tool-surface-session-resume-copy-command-66</guid>
            <pubDate>Sat, 27 Jun 2026 20:55:59 GMT</pubDate>
            <description><![CDATA[Nanocoder v1.28.0 is out. Headline additions: an Agent Client Protocol server so editors like Zed can drive Nanocoder natively, a consolidation of the built-in tool surface from 33 tools down to 19 with automatic profile selection, and session resume that replays the full message and tool-call history into the chat view. Underneath: structured tool-result validation, API-reported context usage, a skill linter, two new first-class providers, and a long list of fixes for local-model workflows.

]]></description>
            <content:encoded><![CDATA[Nanocoder v1.28.0 is out. Headline additions: an Agent Client Protocol server so editors like Zed can drive Nanocoder natively, a consolidation of the built-in tool surface from 33 tools down to 19 with automatic profile selection, and session resume that replays the full message and tool-call history into the chat view. Underneath: structured tool-result validation, API-reported context usage, a skill linter, two new first-class providers, and a long list of fixes for local-model workflows.

Built by the [Nano Collective](https://nanocollective.org) - a community collective building AI tooling not for profit, but for the community.

Full changelog lives in the repo at [https://github.com/Nano-Collective/nanocoder](https://github.com/Nano-Collective/nanocoder).

## Agent Client Protocol (ACP) for Zed and other editors

Nanocoder can now run as an [Agent Client Protocol](https://agentclientprotocol.com) agent:

```bash
nanocoder --acp
```

When launched with `--acp`, Nanocoder speaks JSON-RPC over stdin/stdout and the editor becomes the UI. Streaming text, tool cards, before/after diffs for `string_replace` and `write_file`, permission prompts, model switching, and `ask_user` options all render in the editor instead of the terminal. The four development modes (`normal`, `auto-accept`, `yolo`, `plan`) are exposed as session modes selectable from the editor; sessions start in `auto-accept`.

The new module lives at `source/acp/` and covers the agent, server, session, conversation loop, content conversion, capability negotiation, and permission handling. Thanks to @Avtrkrb. Closes #529.

Follow-up work added the missing ACP docs, kept the parallel tool-execution loop in sync with the plain shell, and made `NANOCODER_MAX_TURNS` configurable (with a raised default) so long ACP and plain-mode runs are not cut short mid-task.

Setup in Zed is two lines in `settings.json`:

```json
{
  "agent_servers": {
    "Nanocoder": {
      "command": "nanocoder",
      "args": ["--acp"]
    }
  }
}
```

Full setup notes and limitations (in-memory session history across editor restarts, selection-only `ask_user`, no image or audio processing) are in the ACP docs at [https://github.com/Nano-Collective/nanocoder](https://github.com/Nano-Collective/nanocoder).

## Tool surface consolidated: 33 down to 19

The built-in tool surface has been consolidated from 33 tools down to 19, with a new auto-detected tune profile as the default. The headline shape changes:

- **Tasks** collapse from four tools into a single `write_tasks` that replaces the whole list. No IDs for the model to juggle, just a current state.
- **File ops** merge `delete`, `move`, `copy`, and `create_directory` into one `file_op`.
- **Git** shrinks from eleven tools to six: `status`, `diff`, `log`, `add`, `commit`, `pr`. Rarer operations flow through `execute_bash`.

On top of the consolidation, the **auto** tune profile is now the default. It infers `full`, `minimal`, or `nano` from the active model's parameter count, so a small local model automatically gets the slim tool set and shortened prompt while a cloud model is unchanged. The profile re-resolves live on model switch and is surfaced in the input indicator (e.g. `tune: nano (auto)`). `/usage` and the context indicator rebuild from the live tune, mode, and model with the profile-filtered tool count, so the numbers reflect what is actually loaded.

Roughly 6.3k lines were removed across this work.

## Structured tool results and single-source validation

Tool arguments are now type-checked against each tool's JSON schema at the execution boundary (`schema-validate.ts`). A malformed model output returns a clear, field-level error the model can self-correct from instead of being coerced at the render layer. Validation runs through a single `withValidation` seam shared by both the native and XML-fallback execution paths, ahead of any per-tool validator.

Approval policy is centralised the same way, and the old global mode context is removed. The current development mode (including a mid-run switch via Shift+Tab) is honoured consistently across the main loop and subagents. Subagents no longer capture `normal` once at startup and silently ignore a later switch.

## Session resume with full history replay

Resuming a saved session now replays the message and tool-call history into the chat view via a new `session-history-renderer`, so you pick up exactly where you left off with the conversation visible rather than an empty screen.

Companion fixes resolved an autosave race, a history-truncation edge case, and resume-after-save paths, all with regression coverage. Thanks to @akramcodez. Closes #545.

## `/copy` for the last assistant response

A new `/copy` slash command copies the most recent assistant response to the system clipboard. Cross-platform via `clipboardy` (pbcopy on macOS, xclip on Linux, clip.exe on Windows). Surfaces a clear warning when there is no assistant message and a real error when the clipboard write fails.

## Safer local-model defaults

Two additions aimed squarely at smaller local models running unattended:

- **Read-before-edit guards.** `string_replace` and `write_file` now require the file to have been read first. A blind edit to a never-read file is rejected at the tool boundary.
- **Tool-call loop detection.** A tool-signature tracker detects a model repeating the same tool call in a tight loop and breaks the cycle.

Both make local models materially safer to leave running on their own.

## Skill linter and command template ergonomics

A new skill linter - `/skills check <name>` plus a `check_skill` tool - validates a bundle from disk using the same parsers the loader uses. A PASS means it will load. It is wired into `/skills create` so the model verifies and self-corrects what it generates. The linter inspects member template bodies too (unsupported mustache tags, unbalanced or unclosed sections, placeholders referencing undeclared parameters), not just frontmatter.

Custom-command bodies now support optional arguments with inline defaults (`name=default`) and conditional sections using mustache's section syntax (`# name` for "if name is set" and `^ name` for "if name is empty"). Inverted-section support in custom-tool templates goes through a shared section engine, so the same rules apply across primitives.

## Two new providers

- **Atlas Cloud** is now a first-class provider and sponsor. Wizard onboarding template and a dedicated provider docs page ship with the release. Atlas Cloud is a full-modal inference platform that exposes LLM, image, and video generation APIs through a single endpoint.
- **Requesty** (https://requesty.ai) is added as a first-class OpenAI-compatible provider, mirroring the OpenRouter integration with a name matcher, app-attribution headers, a wizard onboarding template, and docs. Thanks to @Thibaultjaigu. Closes #589.

## API-reported context usage

The `ctx: NN%` indicator now reflects provider-accurate token counts when the model reports them, captured from the final step's `usage` rather than the tool-loop-summed `totalUsage`. When the provider does not report, Nanocoder falls back to a client-side estimate, marked with a leading `~` (e.g. `ctx: ~42%`); API-reported figures render bare.

Follow-up work anchored the API figure across turns, counted real tool definitions in the auto-compact gate, and added a tiktoken-based generic fallback tokenizer for more accurate counts on models without a native tokenizer. Thanks to @JimStenstrom. Refs #381.

## Smaller things worth knowing

- **Git branch display** in the boot summary and `/status` panel, with narrower-terminal-friendly rendering. Thanks to @ragini-pandey. Closes #539.
- **Provider model discovery** in the wizard now surfaces real discovery errors instead of silently falling back, and shows model names with long labels truncated by ellipsis. Thanks to @akramcodez and @Dhirenderchoudhary. Closes #554.
- **Slash-command completion** supports arrow-key navigation and highlighting, with the double-submit-on-select bug fixed (`TextInput` ignores Enter; `UserInput` handles select-then-submit behind a guard flag) and `customCompletion` restored. Thanks to @rakshith1928. Closes #578.
- **Force-compact-and-retry fallback.** When the LLM server rejects a request for exceeding its context window, Nanocoder now compacts and nudges the conversation to continue instead of failing outright. Thanks to @lordoski. Closes #546.
- **Bash executor** supports timeout, output limits, and abort, with regression tests covering the executor lifecycle. Thanks to @akramcodez. Closes #547.
- **`ask_user` rendering** dropped the leading `?`, fixed spacing and the hanging indent, added arrow-key navigation, and normalised object-shaped options to readable strings (preferring a label over a value id).
- **`@`-file-mention handling** keeps mention placeholders in the chat instead of expanding inline, and large-file inlining is capped to avoid blowing up the prompt.
- **Custom display:** failed tool results condense to a one-liner in compact mode (e.g. `write_file failed.`), while the model still receives the full error in history.

## Fixes

- **Copilot GPT-5 reasoning streams** are now handled correctly via an `@ai-sdk/openai` patch and chat-handler changes, so reasoning content streams properly instead of breaking the response. Thanks to @EntropyParadigm. Closes #577.
- **Propagate `AbortSignal`** to streaming bash execution, so cancelling a turn actually stops a long-running streamed command. Thanks to @Dhirenderchoudhary. Closes #550.
- **Corrupted `agents.config.json` is no longer silently overwritten.** The wizard now honours the `projectDir` prop for config-path resolution and the infinite render loop on the `existingProviders` default param is resolved, all with regression coverage. Thanks to @Dhirenderchoudhary. Closes #551, #552.
- **`read_file` returns an empty marker** instead of throwing on empty files, including range and metadata-only reads. The marker constant is extracted (originally `EMPTY_FILE_MARKER`, later renamed `EMPTY_CONTENT_MARKER`) with tests for files containing only a newline. Thanks to @ragini-pandey. Closes #530.
- **Compaction no longer splits recent tool-call groups**, which previously produced empty model output after compaction.
- **`/` key no longer scrolls the chat view to the bottom** during input. Thanks to @itsishant. Closes #590.
- **Chokidar watcher is closed if daemon startup throws**, and the daemon stop-handler is set up before resources start so a failed lockfile write calls `stop()` and cleans up. Thanks to @OMEE-Y and @rakshith1928. Closes #553, #557.
- A stray `console.log` in `message-queue.tsx` was replaced with the structured logger. Thanks to @A-S-Manoj. Closes #588.
- Subagent context window overrides so delegated agents can run with a different context limit than the main session, and provider names resolve case-insensitively with custom CA bundle support for provider TLS. Thanks to @zerone0x.
- A `tmp >=0.2.6` override resolves a path-traversal advisory in a transitive dependency. Thanks to @ragini-pandey.

## Refactor and dead-code sweep

Beyond the tool-surface consolidation above: unified tool-call ID generation, extracted a shared `useWizardForm` hook, consolidated session-override managers, added message-factory helpers, deduped config loaders, git exec, command dispatch, and conversation-loop flush, extracted `StyledSelectInput` and `makeSimpleToolFormatter`, and deleted several dead modules and orphaned exports (including `fetch-local-models`, orphaned by an earlier removal).

The **Nanocoder Battlemap** competitive comparison and the README and docs have been refreshed.

## Dependency updates

- `ai` 6.0.174 to 6.0.193, plus `@ai-sdk/openai`, `@ai-sdk/openai-compatible`, `@ai-sdk/anthropic`
- `@agentclientprotocol/sdk` 0.22.1 to 0.25.0
- `undici`, `esbuild`
- `@biomejs/biome` 2.5.0
- `lint-staged` 17
- `@types/node`, `@types/vscode`, and other transitive bumps tracked through the lockfile
- `clipboardy ^5.3.1` for `/copy`

If anything is broken or surprising, please open an issue or drop a note in Discord. Thank you for using Nanocoder.]]></content:encoded>
            <category>Nanocoder</category>
        </item>
        <item>
            <title><![CDATA[Nanocoder v1.27.0 - Skills, a daemon, and custom tools]]></title>
            <link>https://nanocollective.org/blog/nanocoder-v1270-skills-a-daemon-and-custom-tools-49</link>
            <guid isPermaLink="false">https://nanocollective.org/blog/nanocoder-v1270-skills-a-daemon-and-custom-tools-49</guid>
            <pubDate>Tue, 02 Jun 2026 15:21:24 GMT</pubDate>
            <description><![CDATA[Nanocoder v1.27.0 is out. This release is the biggest extension surface the project has shipped: a unified extension primitive called Skills, a per-project background daemon for event-triggered runs, and a markdown-defined custom tools layer. Also: two OOM fixes, a startup config lint, and a reworked OpenRouter model picker.

Built by the [Nano Collective](https://nanocollective.org) - a community collective building AI tooling not for profit, but for the community.

## Skills: a unified ext]]></description>
            <content:encoded><![CDATA[Nanocoder v1.27.0 is out. This release is the biggest extension surface the project has shipped: a unified extension primitive called Skills, a per-project background daemon for event-triggered runs, and a markdown-defined custom tools layer. Also: two OOM fixes, a startup config lint, and a reworked OpenRouter model picker.

Built by the [Nano Collective](https://nanocollective.org) - a community collective building AI tooling not for profit, but for the community.

## Skills: a unified extension primitive

Nanocoder has had custom commands, subagents, and tools as separate extension types. v1.27.0 replaces that with **Skills**: a single surface that brings all three under one ergonomic model.

A skill member lives in `.nanocoder/commands|agents|tools/` as a `.md` file - fully backwards-compatible with how those directories already work. The new behaviour is the **bundle** form: a directory under `.nanocoder/skills/<name>/` with a `skill.yaml` manifest and optional `commands/`, `agents/`, `tools/` subdirectories. Bundles ship and version together as a unit. A bundle's subagent automatically gets its sibling tools, scoped tools are hidden from the global tool list, and bundle commands auto-namespace (`commands/status.md` in bundle `git` invokes as `/git:status`).

New `/skills` slash command lists everything loaded. Bundled members fan out into the existing `CustomCommandLoader`, `SubagentLoader`, and `ToolManager.registry` - downstream consumers keep using their familiar registries.

## A per-project daemon for event-triggered skill runs

Skill members can now declare `subscribe:` blocks in their frontmatter (or a bundle manifest) that wake them on `file.changed` and `schedule.cron` events. The daemon handles dispatch.

Start it with `nanocoder daemon start` (stop, status, and logs are also available). It runs as a long-lived background process with a lockfile, Unix-socket IPC, and ready-to-use installers for launchd (macOS) and systemd user units (Linux). The interactive TUI never starts event sources - file watching and cron only run when you opt in via the daemon.

Triggered runs execute in a new internal **headless** mode (no `ask_user`, no foreground confirmations). Per-subscription `confirm: true` opts into `plan` mode instead. The daemon backpressure-caps per-subscription concurrency and trailing-debounces `file.changed` by 500 ms so chatty save loops do not pile up.

## The legacy scheduler is gone

The old `source/schedule/` module (runner, storage, index, types) has been removed. Schedules are now expressed as `schedule.cron` subscriptions on a skill member. The old `useSchedulerMode` hook, `scheduler-view` component, and `.nanocoder/schedules.json` / `.nanocoder/schedules/*.md` storage layout are gone. The `/schedule` command has been trimmed and refocused.

## Custom Tools: markdown-defined, model-callable tools

Custom Tools sit between custom commands (prompt injection only) and full MCP servers (external process, full protocol). Drop a `.md` into `.nanocoder/tools/` (project) or `~/.config/nanocoder/tools/` (personal), declare parameters in YAML frontmatter with JSON Schema-style types, and write a shell command body using template placeholders. All substitutions are shell-quoted to prevent injection.

Here is the full shape of a Custom Tool definition:

```markdown
---
name: git_review
description: "Run git diff and collect changed files for review"
parameters:
  - name: since
    type: string
    required: true
    description: "Show changes more recent than this ref"
approval: never
read_only: true
---
git diff --name-only &#123;&#123;since&#125;&#125;..HEAD
```

Includes `approval` (`always`/`never`) and `read_only` flags that participate in mode policy: plan mode requires `approval=never && read_only=true`, scheduler/headless requires `approval=never`. New `/tools create <name>` scaffolds a template and asks the model to help fill it in. Project tools shadow personal ones by name, and they register into the same `ToolManager` registry as built-ins and MCP tools.

## LLM-based context compaction

The default `/compact` strategy is now LLM-based. The active model writes a structured markdown summary of the compressible segment (Context / Decisions / Files modified / Tools used / Open questions) using a dedicated summariser prompt, replacing the older messages with one synthetic summary. Recent messages are kept verbatim. Higher fidelity than mechanical truncation at the cost of one extra round-trip.

New `strategy` field in `autoCompact` config (`llm` default, `mechanical` legacy), new CLI flags (`--llm`, `--mechanical`, `--strategy <name>`), and automatic fallback to mechanical compression on LLM failure (network error, empty response, summary larger than original).

## OpenRouter request configuration

A new `openrouter` block on the provider config forwards OpenRouter-specific request body fields on every request: provider routing rules, reasoning, plugins, models fallback list, `service_tier`, `route`, `user`, and so on. Always-on transport and routing concerns, not gated by `/tune`. Provider is detected by name (case-insensitive). `tune.modelParameters.reasoningEffort` populates `openrouter.reasoning.effort` when unset; explicit values on the provider config always win.

## Config lint at startup

Startup now surfaces common misconfigurations as warnings before they cause silent failures: an `openrouter` block on a provider that is not OpenRouter, unknown fields, type mismatches. Includes a full test suite.

## OpenRouter model selection reworked

The provider wizard's model selection list has been replaced with a paginated, searchable `ModelSelectionList` (12 visible items, page navigation, multi-select with running counter, select-all, error state). Browsing OpenRouter's hundreds of models is now usable from the wizard.

## Unified Session Service

A new Session Service consolidates the various ad-hoc keying strategies scattered across commands, handlers, and tool result display. Touched 50 files, removing duplicated key-derivation logic and shrinking `useAppState` by ~38 lines.

## Nanocoder Battlemap

A new long-form competitive comparison doc covers Nanocoder against Claude Code, OpenAI Codex CLI, Gemini CLI, Aider, OpenCode, Crush, and Pi across twelve axes: license, pricing, local-model support, MCP, extensibility, tool-calling, subagents, surface, plain mode, stars, contributors, and telemetry. Honest where Nanocoder leads, honest where it does not.

## Fixes

- **Streaming OOM on long responses.** `StreamingMessage` was calling `wrapWithTrimmedContinuations()` on the entire growing assistant message on every ~150 ms flush. A ~37k-token response would overwhelm the GC and crash the 4 GB Node heap. Now slices to a bounded tail before wrapping so per-render work is constant. Thanks to @abhishekDeshmukh74.

- **Performance entry buffer OOM in long sessions.** React 19's `react-reconciler` (dev build) and Node's built-in fetch (undici) both write to the global `performance` buffer, which nanocoder was never draining. Over long subagent-heavy sessions the buffer accumulated millions of entries until V8 thrashed in mark-compact GC. Fix installs an unref'd 30s interval that clears marks and measures in the Ink render path. Thanks to @ragini-pandey.

- Auto-compact now respects the per-provider `contextWindow` override instead of computing the threshold against the model's default context window.

- Errors from AI SDK providers now surface meaningful messages (status codes, body excerpts, nested fields) instead of `[object Object]`.

- Removed dead MCP server discovery hosts from the wizard templates (`remote.mcpservers.org` etc.). Companion guard in `mcp-client` prevents the wizard from surfacing entries it cannot reach.

- Markdown parser regressions fixed: indented fenced code blocks, fenced blocks inside blockquotes, and indented list continuations all extract correctly now. Indented-code extraction removed from the shared parser - only fenced blocks split out for copyable rendering.

- Code blocks are now copyable without surrounding ASCII box borders that previously got copied into the clipboard. Thanks to @aravindinduri.

- System prompt tightened to reduce over-eager task list creation on small jobs and to avoid markdown tables in model output (they render poorly in the terminal).

- Nix packaging updated for pnpm 11 via `pkgs.pnpm_11`. Drops version-override scaffolding and postInstall hacks. Re-enables the `update-nix.yml` workflow.

Full changelog at [https://github.com/Nano-Collective/nanocoder/blob/main/CHANGELOG.md](https://github.com/Nano-Collective/nanocoder).]]></content:encoded>
            <category>Nanocoder</category>
        </item>
        <item>
            <title><![CDATA[get-md v1.5.0: Pluggable LLMs, batch crawling, and RAG helpers]]></title>
            <link>https://nanocollective.org/blog/get-md-v150-pluggable-llms-batch-crawling-and-rag-helpers-47</link>
            <guid isPermaLink="false">https://nanocollective.org/blog/get-md-v150-pluggable-llms-batch-crawling-and-rag-helpers-47</guid>
            <pubDate>Tue, 26 May 2026 20:42:56 GMT</pubDate>
            <description><![CDATA[
get-md v1.5.0 is out. This is the biggest release since v1.0, with five new capabilities stacked on top of a full surface audit that fixed a dozen small issues. Here's what changed.

Built by the [Nano Collective](https://nanocollective.org), a community collective building AI tooling not for profit, but for the community.

## Pluggable LLM backend

v1.5.0 makes the LLM backend pluggable instead of hard-wired to local `ReaderLM-v2`. It now routes through whichever provider you configure,]]></description>
            <content:encoded><![CDATA[
get-md v1.5.0 is out. This is the biggest release since v1.0, with five new capabilities stacked on top of a full surface audit that fixed a dozen small issues. Here's what changed.

Built by the [Nano Collective](https://nanocollective.org), a community collective building AI tooling not for profit, but for the community.

## Pluggable LLM backend

v1.5.0 makes the LLM backend pluggable instead of hard-wired to local `ReaderLM-v2`. It now routes through whichever provider you configure, using the same `sdkProvider` shape as [Nanotune](https://github.com/Nano-Collective/nanotune) and [Nanocoder](https://github.com/Nano-Collective/nanocoder), so one config covers the whole Nano Collective stack.

Supported providers: `openai-compatible` (Ollama, OpenRouter, Together, Groq, LM Studio, OpenAI, vLLM), `anthropic`, `google`, and `local-llama`. Peer dependencies are optional. Install only the provider you actually use. Missing peer deps surface a clear "install this" error instead of `ERR_MODULE_NOT_FOUND`.

Config files now support `${ENV_VAR}` and `${VAR:-default}` substitution, so `apiKey` values stay out of committed files. CLI flags `--llm-provider`, `--llm-base-url`, `--llm-model`, and `--llm-api-key` handle everything. Run `--show-config` to inspect your current setup; API keys are redacted.

Default behaviour is unchanged: existing setups with `useLLM: true` and no explicit provider continue to use `local-llama` as before.

## Batch mode

Two new API functions handle bulk URL conversion:

```typescript
// Yields per-URL results as they complete
const batch = convertBatch(urls, { concurrency: 5 });
for await (const result of batch) {
  console.log(result.markdown);
}

// Convenience: buffers everything into an array
const all = await convertBatchAll(urls, options);
```

CLI batch mode reads a file of URLs (one per line, `#` comments and blanks stripped), writes one `.md` per URL to an output directory, and produces a JSON manifest summary alongside. Concurrency, error handling, file naming patterns, and JSONL output are all configurable from the CLI.

## Sitemap crawling

`parseSitemap` fetches and parses a sitemap URL, recursively follows `<sitemapindex>` entries, and returns a flat list of all discovered URLs. `convertSitemap` composes that with the batch converter for a complete crawl-and-convert pipeline.

CLI usage:

```bash
getmd --sitemap https://example.com/sitemap.xml \
  --include "**/blog/**" \
  --exclude "**/drafts/**" \
  --max-depth 3 \
  --max-urls 10000
```

Glob filtering applies with `*` matching no slashes and `**` matching any characters. Default max depth is 3; default max URL count is 10000.

## RAG ingestion helpers

Two new exports make `get-md` composable as a RAG ingestion building block:

`chunkMarkdown(md, { maxTokens, overlap?, includeHeadingPath? })` splits markdown at heading boundaries. Each chunk carries the full heading path from root to its parent heading, prepended as an introductory line on continuation chunks. Overlap controls how many chunks carry a trailing overlap window from the previous chunk.

`estimateTokens(text)` provides a chars/4 heuristic for quick context-window budgeting. It is also surfaced automatically on `ConversionStats.estimatedTokens` for every single-URL conversion, so you get token estimates without any extra calls.

Both are available as standalone exports and are documented in the new API utilities guide.

## Image localisation

`convertToMarkdown` now accepts a `downloadImages: '<dir>'` option. It downloads referenced images in parallel, rewrites their `src` in the markdown to point at the local copies, and handles per-image failures gracefully. A single failed image prints a warning but never aborts the conversion. URL deduplication means a shared image referenced across multiple pages is downloaded once.

The CLI mirrors this with `--download-images <dir>`. When the positional input is a URL, the CLI now sets that URL as the implicit `baseUrl` so relative image refs (`/images/logo.svg`) resolve correctly without needing `--base-url`.

The HTML cleaner preserves lazy-load attributes (`data-src`, `data-original`, `data-lazy-src`, `srcset`) and resolves them against the base URL, which means Wikipedia, Medium, Substack, and most modern blog platforms produce complete image references instead of 1x1 placeholders.

## HTTP cache and retry

Transient failure handling is now built in. The converter retries on network errors, 5xx responses, and 429 rate-limit responses. Exponential backoff with up to 25% jitter is applied. On 429, it reads the `Retry-After` header and respects it (handles both the seconds form and HTTP-date).

An opt-in file-system cache (`~/.get-md/cache` or a custom path) skips the network entirely on cache hits. TTL is configurable per-call. Cache failures fall back to a live fetch rather than throwing, so the cache is always non-blocking.

CLI flags: `--retries`, `--retry-delay`, `--cache`, `--cache-dir`, `--cache-max-age <seconds>`.

## Safer fetches

A `maxBytes` option (default 10MB) caps what the fetcher will accept. If `Content-Length` declares more, the fetch is aborted before downloading. If a server omits or lies about the header, the stream is aborted mid-flight before unbounded buffering can occur. Combined with the retry and cache layers, the fetch path is now hardened against hostile or misbehaving sources.

## Image and output path fixes

Two longstanding rough edges are smoothed. Nested output paths like `--output ./nested/file.md` now auto-create the parent directory instead of crashing. And the model description copy that previously said "986MB" (the old legacy model) is corrected to "1.12GB" everywhere.

## Documentation

New API reference pages: `docs/api/batch.md`, `docs/api/sitemap.md`, and `docs/api/utilities.md`. The API index (`docs/api/index.md`) is restructured with every public export linked. The Conversion API page now uses "Conversion API" as its section label while keeping the same URL slug so existing deep links stay valid.

New guides: `docs/guides/remote-llm.md`, `docs/guides/batch.md`, and `docs/guides/sitemap.md`. Manual smoke-test crib sheets live in the repo root: `SMOKE_TESTS.md`, `CRAWL_SITEMAP.md`, `TEST_IMAGES.md`.

## Tests

528 tests (up from 405 at 1.4.1). New coverage for retry on 5xx/429/network errors, HTTP cache hit/miss/TTL, image localisation with relative refs and baseUrl, protocol-relative URLs, sitemap parsing (flat and nested), batch concurrency, chunking at heading boundaries, env-vars in config, and JSON output in both single and JSONL modes.

## What was fixed

- `--version` now reads from `package.json` at runtime instead of printing a hardcoded value.
- `llmMaxTokens` default lowered to 8192. The documented 512000 was always capped at 8192 internally. The fix is accuracy rather than behaviour.
- Stale `dist/parsers/json-parser.*` removed. `ajv` dependency dropped (unused in `src/`).
- Custom-rule state leak fixed: `MarkdownParser` now constructs a fresh `TurndownService` per `convert()` call.
- `Required<MarkdownOptions>` cast replaced with a proper `NormalizedMarkdownOptions` type, closing a type hole.
- `-o ./nested/file.md` now creates parent directories.

## Upgrading

Upgrade from npm:

```bash
npm install @nanocollective/get-md@latest
```

Or with pnpm:

```bash
pnpm add @nanocollective/get-md@latest
```

Node.js 22 or later is required. If you use the LLM path, check the new [Remote LLM Providers guide](docs/guides/remote-llm.md) if you want to switch away from the local model.

If you hit any issues or want to discuss the new features, open an issue or find us on [Discord](https://discord.gg/ktPDV6rekE).

Repo: https://github.com/Nano-Collective/get-md
]]></content:encoded>
            <category>Packages</category>
        </item>
        <item>
            <title><![CDATA[Nanocoder v1.26.0 is out. Here is what changed and why it matters.]]></title>
            <link>https://nanocollective.org/blog/nanocoder-v1260-is-out-here-is-what-changed-and-why-it-matters-43</link>
            <guid isPermaLink="false">https://nanocollective.org/blog/nanocoder-v1260-is-out-here-is-what-changed-and-why-it-matters-43</guid>
            <pubDate>Mon, 11 May 2026 22:25:07 GMT</pubDate>
            <description><![CDATA[## Nano mode: a leaner prompt for low-end hardware

The `/tune` command now offers a third tool profile called **nano mode**. It is strictly more aggressive than minimal: it drops `find_files`, `list_directory`, and `agent` from the tool set; trims `CORE PRINCIPLES` and `CODING PRACTICES` sections; uses 4-line-or-less `TASK APPROACH`, `FILE OPERATIONS`, and `CONSTRAINTS` blocks; and replaces the verbose `SYSTEM INFORMATION` block with a single-line `## SYSTEM` note.

The result is a system p]]></description>
            <content:encoded><![CDATA[## Nano mode: a leaner prompt for low-end hardware

The `/tune` command now offers a third tool profile called **nano mode**. It is strictly more aggressive than minimal: it drops `find_files`, `list_directory`, and `agent` from the tool set; trims `CORE PRINCIPLES` and `CODING PRACTICES` sections; uses 4-line-or-less `TASK APPROACH`, `FILE OPERATIONS`, and `CONSTRAINTS` blocks; and replaces the verbose `SYSTEM INFORMATION` block with a single-line `## SYSTEM` note.

The result is a system prompt of roughly 150-250 tokens, compared to 500-700 in minimal mode. That difference matters when you are running a 1B or 3B parameter model on a machine with limited VRAM or RAM.

Nano mode ships with a "Nano (low-end hardware)" preset and an **Include AGENTS.md** toggle so you can opt back in if your setup handles it.

## Reasoning traces, live

Nanocoder now streams reasoning content from models that emit it, such as Codex GPT-5, DeepSeek-R1-style models, Anthropic extended thinking, and similar. It renders as a collapsible **Thought** block above the response, persists across the conversation history, and appears in logs.

Toggle expansion with `Control+R`. Set the default via the new **Display Settings** panel (`/settings` → "Tool Results and Thinking"). Pin it per-session from the tune menu using the `expandedReasoning` option.

`Control+R` also works for reasoning trace follow-ups, and the system ensures reasoning content survives correctly across the full conversation lifecycle.

## Non-interactive `--plain` flag

A new `--plain` flag streams output without Ink rendering, making it safe for CI pipelines, shell scripts, and programmatic pipes. Exit codes are deterministic, stdin and stdout are handled cleanly, and there are no interactive prompts.

## VS Code extension rework

The "Ask Nanocoder" command is gone. Instead, highlighted text and the currently focused file are automatically pulled into Nanocoder as context, a more natural interaction model. The extension protocol and entrypoint have been rewritten, and the development mode indicator and chat input hooks are tighter.

## Other additions

- `/rename` — rename the current chat session. Reflected in the development mode indicator.
- `defaultMode` — set a starting mode (`normal`, `auto-accept`, `yolo`, `plan`) in `agents.config.json` so you do not always launch in normal mode.
- Custom system prompt support via `agents.config.json`. Define a project-level prompt that augments or replaces built-in sections.
- Per-provider and per-model **context window overrides** via `contextWindow` and `contextWindows[model]`. Resolution order: session override (`/context-max`, `--context-max`) → provider model override → provider default → `NANOCODER_CONTEXT_LIMIT` → fallback. `/usage`, status bar, auto-compact, and context reporting all use the active provider config.
- **Subagent context window overrides** — delegated agents can run with a different context limit than the main session.
- `disabledTools` — disable specific built-in tools project-wide, honoured by both main agent and subagents.
- `--trust-directory` — bypass the first-run directory trust prompt for automation. Yolo mode already skips the validator.
- JSON tool fallback for non-tool-calling open-weights models (Qwen, Kimi, GLM). The tune menu cycles through Native ON / OFF (XML) / OFF (JSON). Includes hallucination recovery so reasoning content does not leak into rendered output.
- `<function=...>` format support in the tool-call parser.
- **Display Settings** panel — new toggles for "Show Thinking by default" and "Expand Tool Results by default", persisted to `nanocoder-preferences.json`.
- 12+ new bundled themes.

## Toolchain and configuration updates

- Node.js 22 + pnpm 11 across CI, devcontainer, and `engines.node`. `packageManager` field pins the pnpm version automatically for contributors and CI.
- `pnpm-workspace.yaml` replaces `package.json`-based pnpm config. All 16 version overrides were removed — every one was redundant under current dependency resolution.

## Fixes

- Empty model responses now trigger a capped auto-continue instead of looping or crashing.
- Context-size failures surface explicitly rather than silently retrying empty responses.
- Compressed messages persist across recursive conversation turns.
- Stream errors are thrown immediately rather than swallowed.
- Malformed XML retries are capped to prevent OOM on stuck models.
- `lastProvider` state is now correct after running the config wizard mid-session.
- File search, content search, and `@mention` autocomplete work cross-platform on Windows.
- `MCPTool.inputSchema` typed as `JSONSchema7` with an `isPlainObject` guard at the protocol boundary.
- `caCertPath` TLS configuration now applies to Anthropic and Google providers.
- Provider names resolved case-insensitively throughout.
- Quoted custom command arguments now parse as a single parameter.
- `git_pr` tool no longer requires approval in yolo mode.
- Various rendering, scheduling, and test/CI regressions fixed.

## Dependency updates

`ai` 6.0.116 → 6.0.174, `@ai-sdk/openai` 3.0.41 → 3.0.53, `@ai-sdk/google` 3.0.53 → 3.0.64, `@ai-sdk/openai-compatible` 2.0.35 → 2.0.41, `undici` 8.0.2 → 8.2.0, `react` 19.2.4 → 19.2.6, `yaml` 2.8.3 → 2.8.4, `dotenv` 17.3.1 → 17.4.2, `@nanocollective/get-md` 1.3.0 → 1.3.1, plus dev-dependency bumps.

## Breaking change

`nanocoderTools.alwaysAllow` has been removed. It duplicated the top-level `alwaysAllow` with no extra behaviour. Move any entries from `nanocoderTools.alwaysAllow` to the top-level `alwaysAllow` array in `agents.config.json`. The deprecated `agents.config.example.json` has also been removed.

---

Built by the [Nano Collective](https://nanocollective.org) — a community collective building AI tooling not for profit, but for the community.

https://github.com/Nano-Collective/nanocoder
]]></content:encoded>
            <category>Nanocoder</category>
        </item>
        <item>
            <title><![CDATA[Nanocoder 1.25.0: Introducing Yolo Mode, Subagents, and Smart Tool Orchestration]]></title>
            <link>https://nanocollective.org/blog/nanocoder-1250-introducing-yolo-mode-subagents-and-smart-tool-orchestration-39</link>
            <guid isPermaLink="false">https://nanocollective.org/blog/nanocoder-1250-introducing-yolo-mode-subagents-and-smart-tool-orchestration-39</guid>
            <pubDate>Wed, 15 Apr 2026 15:18:13 GMT</pubDate>
            <description><![CDATA[We're excited to announce Nanocoder 1.25.0, a major release that adds some amazing new features to how you collaborate with AI in your development workflow. This version introduces powerful new modes, intelligent subagent coordination, and smarter tool management—all designed to keep you in flow and reduce friction.

## Introducing Yolo Mode

Sometimes you know exactly what you need done, and the confirmation dialogs just slow you down. **Yolo mode** is here for those moments. This new devel]]></description>
            <content:encoded><![CDATA[We're excited to announce Nanocoder 1.25.0, a major release that adds some amazing new features to how you collaborate with AI in your development workflow. This version introduces powerful new modes, intelligent subagent coordination, and smarter tool management—all designed to keep you in flow and reduce friction.

## Introducing Yolo Mode

Sometimes you know exactly what you need done, and the confirmation dialogs just slow you down. **Yolo mode** is here for those moments. This new development mode auto-accepts *every* tool without exception - previously auto-accept mode (still available) did reduce what needed to be accepted but things like bash execution still required approval.

**Warning though**: this means every tool, no matter what is executed without approval including potentially destructive ones. Nanocoder has basic built in validation but it won't catch everything - proceed with caution.

Toggle between normal → auto-accept → yolo → plan modes with **Shift+Tab**. The status bar turns red when yolo mode is active, so you'll never forget you're operating without guardrails. Perfect for rapid prototyping, experimental refactoring, or when you're happy to walk away and let Nanocoder work with full trust.

## Subagents: Parallel Problem-Solving

Complex tasks often benefit from delegation. With subagents, Nanocoder can now spawn isolated child conversations to handle specific work in parallel. We ship with two built-in agents:

- **Explore**: Read-only codebase investigation perfect for understanding unfamiliar code paths
- **Reviewer**: Code review with actionable, constructive feedback

Subagents have independent tool sets tailored to their purpose, and their progress renders live in-place as they work. You can also define custom subagents by dropping markdown files with YAML frontmatter in `.nanocoder/agents/`. Manage them all via the `/agents` command.

We built subagents to be flexible with many configuration options and we're excited to build out this part of ecosystem further.

## Smarter Tool Orchestration

We've completely redesigned the system prompt into modular, composable sections. Instead of one monolithic file, the prompt now assembles dynamically based on your mode—normal, auto-accept, plan, and scheduler each get tailored instructions with only the tools prompts relevant to that context.

On top of that, we've built the `/tune` command, giving you fine-grained per-session control: toggle between full and minimal tool profiles, disable native tools to force XML fallback, or enable aggressive compact mode. Everything persists for your session - this helps Nanocoder work well with smaller models, a key mission of ours.

Lastly., **Plan mode is now actually useful**: it enforces read-only tools at the policy level, blocking all mutation tools (write, bash, git commits, task management) while keeping exploration tools available. This forces Nanocoder to thoroughly investigate and then propose a plan before any changes are made.

## Provider and Configuration Improvements

- **ChatGPT Codex**: New provider with OAuth device flow authentication (`/codex-login`), streaming responses, and dedicated credential management
- **Web search**: Migrated from scraping to the official Brave Search API for reliability and compliance
- **Provider wizard**: Now auto-detects API compatibility and fetches available models from any endpoint
- **`/setup-config`**: New command that lists all your config files and opens them in your editor

## Polish and Performance

- Desktop notifications for tool confirmations, question prompts, and generation completions (macOS, Linux, Windows)
- Startup time reduced through parallelized initialization
- Tool output truncation now respects terminal width across all tools
- Live in-place task list updates instead of repeated static lists
- Improved Ollama timeout handling for slow local models

## Under the Hood

We fixed several important bugs including the `alwaysAllow` config not being respected, a dim color accessibility issue, and a scheduler mode memory leak. We also added comprehensive debug logging to 15 previously silent error catches in git utilities.

Nanocoder 1.25.0 is available now. Head to the GitHub to download and view docs: https://github.com/Nano-Collective/nanocoder

Happy coding! 🚀
]]></content:encoded>
            <category>Nanocoder</category>
        </item>
        <item>
            <title><![CDATA[Nanocoder 1.24.0: Parallel Tool Execution and Enhanced Configuration]]></title>
            <link>https://nanocollective.org/blog/nanocoder-1240-parallel-tool-execution-and-enhanced-configuration-36</link>
            <guid isPermaLink="false">https://nanocollective.org/blog/nanocoder-1240-parallel-tool-execution-and-enhanced-configuration-36</guid>
            <pubDate>Wed, 18 Mar 2026 18:25:02 GMT</pubDate>
            <description><![CDATA[We're excited to announce Nanocoder 1.24.0, bringing significant improvements to performance, flexibility, and user experience. This release includes parallel tool execution, enhanced CLI integration, and streamlined configuration management.

## What's New

### Parallel Tool Execution
The biggest performance improvement in this release is **parallel tool execution**. Previously, tools were executed sequentially, which could slow down complex operations. Now, when your model identifies inde]]></description>
            <content:encoded><![CDATA[We're excited to announce Nanocoder 1.24.0, bringing significant improvements to performance, flexibility, and user experience. This release includes parallel tool execution, enhanced CLI integration, and streamlined configuration management.

## What's New

### Parallel Tool Execution
The biggest performance improvement in this release is **parallel tool execution**. Previously, tools were executed sequentially, which could slow down complex operations. Now, when your model identifies independent tool calls, Nanocoder executes them concurrently. This means faster task completion for workflows that involve reading multiple files, running several bash commands, or searching across different resources.

### Session Resumption with `/resume`
Lost a productive chat session? No problem. The new `/resume` command lets you restore previous conversations. Sessions are automatically saved and presented in an interactive selector, filtered by your current project directory (with an `--all` flag to view global sessions). This is perfect for long-running tasks or picking up where you left off.

### CI/CD and Automation Support
Nanocoder is now more scriptable:
- **`--provider` and `--model` CLI flags** skip the interactive setup wizard, allowing CI/CD pipelines to specify providers and models directly
- **`NANOCODER_PROVIDERS` environment variable** enables Docker containers and CI environments to configure providers without config files

Example usage:
```bash
nanocoder --provider anthropic --model claude-opus-4-6 "analyze this code"
```

### Enhanced Provider Support
- **GitHub Copilot** now available as a provider with OAuth device flow authentication and a new `/copilot-login` command
- **MLX Server** template added for local Apple Silicon inference
- Support for **VS Code forks** (Cursor, Windsurf, VSCodium) in IDE integration

### Configuration Cleanup
This release includes breaking changes to modernize configuration:
- Legacy `~/.agents.config.json` and `~/.nanocoder-preferences.json` paths have been removed
- MCP server configuration now uses platform-specific directories (`.mcp.json`)
- These changes ensure cleaner, more maintainable configurations aligned with XDG standards

### UI and UX Improvements
- **Compact mode toggle** (Ctrl+O) collapses the conversation view for focused editing
- **Aurora Borealis theme** added to the theme collection
- **Tool approval redesign** using AI SDK's human-in-the-loop pattern for improved reliability
- **XML fallback notice** informs users when models fall back to XML tool calls

### Under the Hood
- Simplified tool processing by consolidating parsers and reducing XML double-parsing
- Restructured documentation into Nextra-compatible format with better organization
- Refactored tool system with cleaner code architecture
- Fixed critical issues with MCP configuration loading and provider timeouts

## Try It Now

Thanks for being a part of the Nano Collective community and for using Nanocoder! Install or update to 1.24.0 today. If you're new to Nanocoder, head to [Releases](https://github.com/Nano-Collective/nanocoder/releases/tag/v1.24.0) to get started.

Have feedback or run into issues? Reach out on Discord or open an issue on GitHub. Happy coding!
]]></content:encoded>
            <category>Nanocoder</category>
        </item>
        <item>
            <title><![CDATA[Nanocoder 1.23.0: Interactive Prompts, Scheduled Tasks, and Enhanced Stability]]></title>
            <link>https://nanocollective.org/blog/nanocoder-1230-interactive-prompts-scheduled-tasks-and-enhanced-stability-35</link>
            <guid isPermaLink="false">https://nanocollective.org/blog/nanocoder-1230-interactive-prompts-scheduled-tasks-and-enhanced-stability-35</guid>
            <pubDate>Tue, 10 Mar 2026 00:42:39 GMT</pubDate>
            <description><![CDATA[We're very happy to announce Nanocoder 1.23.0 - a major release bringing powerful new features for automation, interactivity, and reliability. Whether you're running scheduled tasks or building more sophisticated AI workflows, this release delivers significant improvements.

## Interactive User Prompts

The new `ask_user` tool enables your AI agent to pause and ask questions during a conversation. The model can now present selectable options and wait for your response before continuing. This]]></description>
            <content:encoded><![CDATA[We're very happy to announce Nanocoder 1.23.0 - a major release bringing powerful new features for automation, interactivity, and reliability. Whether you're running scheduled tasks or building more sophisticated AI workflows, this release delivers significant improvements.

## Interactive User Prompts

The new `ask_user` tool enables your AI agent to pause and ask questions during a conversation. The model can now present selectable options and wait for your response before continuing. This transforms Nanocoder into a true interactive partner - perfect for decision-making workflows where the AI needs human input to proceed intelligently.

```
Agent: Should I create a new branch for this feature?
Options:
  1. Yes, create "feature/new-ui"
  2. Yes, create "bugfix/improve-performance"
  3. No, work on main

Your choice: 1
```

## Scheduled Task Automation

Running the same tasks repeatedly? The new per-project cron scheduler is built for you. Store markdown prompts with YAML frontmatter in `.nanocoder/schedules/`, then manage them with the `/schedule` command:

```bash
/schedule create "Daily code review"
/schedule add "0 9 * * MON" "Review pull requests and test coverage"
/schedule start
```

Tasks run on a schedule with full run history, sequential job execution, and intelligent deduplication. Perfect for automated testing, report generation, or daily health checks.

## Graceful Shutdown and Reliability

Behind the scenes, we've added a centralized `ShutdownManager` that coordinates cleanup of all services—VS Code integration, MCP servers, LSP managers—preventing orphaned processes and dangling connections. Combined with our V2 type-safe tool system overhaul and three-tiered defense against chaotic LLM outputs, Nanocoder now delivers significantly more stable behavior, especially with local LLMs.

## Additional Improvements

- **New file operations**: `delete_file`, `move_file`, `create_directory`, `copy_file` tools for comprehensive file management
- **Enhanced text input**: Readline-style keybinds (Ctrl+W, Ctrl+U, Ctrl+K, Ctrl+A/E, Ctrl+B/F) for smoother CLI editing
- **Context awareness**: `/context-max` command and env variable for manual context length control on unlisted models
- **Better searches**: `search_file_contents` now supports `include` and `path` parameters for scoped queries
- **Visual enhancements**: Kanagawa theme, persistent context percentage display in mode indicator
- **Unified commands**: Refactored skills system into cleaner custom commands with optional metadata
- **Robust parsing**: XML parser optimizations and comprehensive JSON tool call parsing with self-correction

## What's Under the Hood

This release includes 40+ changes improving stability, security, and developer experience. We've strengthened error handling, optimized bash output streaming for better responsiveness, fixed local Ollama server detection, and updated core dependencies for security and compatibility.
]]></content:encoded>
            <category>Nanocoder</category>
        </item>
        <item>
            <title><![CDATA[New Package: Type-safe JSON migration tool with Zod validation]]></title>
            <link>https://nanocollective.org/blog/new-package-type-safe-json-migration-tool-with-zod-validation-34</link>
            <guid isPermaLink="false">https://nanocollective.org/blog/new-package-type-safe-json-migration-tool-with-zod-validation-34</guid>
            <pubDate>Sun, 01 Mar 2026 23:24:45 GMT</pubDate>
            <description><![CDATA[When you store JSON locally, the structure inevitably changes over time. New fields get added, old ones get renamed, formats evolve. Without a proper migration system you end up with scattered `if` statements checking for old formats, or worse, data that silently breaks when the shape changes underneath you.

We've been dealing with this ourselves across a few projects and figured it was time to build something small and focused to handle it properly.

---

**Now Released**: a type-safe JS]]></description>
            <content:encoded><![CDATA[When you store JSON locally, the structure inevitably changes over time. New fields get added, old ones get renamed, formats evolve. Without a proper migration system you end up with scattered `if` statements checking for old formats, or worse, data that silently breaks when the shape changes underneath you.

We've been dealing with this ourselves across a few projects and figured it was time to build something small and focused to handle it properly.

---

**Now Released**: a type-safe JSON migration library for TypeScript - [@nanocollective/json-up](https://github.com/Nano-Collective/json-up)

---

## The idea

Define a chain of versioned migrations, each with a Zod schema and a transform function. When you call `migrate()`, it reads the current version from your data, runs every migration after that version in order, and validates the result at each step. You get full TypeScript inference throughout the chain, so if your transform returns the wrong shape, you know at compile time rather than at runtime.

```typescript
const migrations = createMigrations()
  .add({
    version: 1,
    schema: z.object({ name: z.string() }),
    up: (data) => ({ name: data.name ?? "Unknown" }),
  })
  .add({
    version: 2,
    schema: z.object({ firstName: z.string(), lastName: z.string() }),
    up: (data) => {
      const [firstName = "", lastName = ""] = data.name.split(" ");
      return { firstName, lastName };
    },
  })
  .build();

const result = migrate({
  state: { _version: 1, name: "Jane Doe" },
  migrations,
});
// { _version: 2, firstName: "Jane", lastName: "Doe" }
```

It handles the common patterns you would expect: renaming fields, adding defaults, restructuring nested objects, transforming arrays, and migrating from unversioned legacy data. There are three distinct error types (`ValidationError`, `MigrationError`, `VersionError`) so you can handle failures precisely rather than catching a generic error and guessing.

## What it looks like in practice

- **Fluent builder API** for chaining migration steps together
- **Zod validation at every step**, not just the final output
- **Automatic version tracking** via a configurable version key (defaults to `_version`)
- **Full TypeScript inference** across the entire migration chain
- **Zero runtime dependencies** - only Zod as a peer dependency
- **Works with Zod v3 and v4**

The whole thing installs with:

```bash
npm install @nanocollective/json-up zod
```

> [!TIP]
> If your data already uses a field like `version` or `schemaVersion`, you can pass a custom `key` option to `migrate()` instead of the default `_version`.

## Documentation

We've written up proper docs covering the full API, error handling, and common migration patterns:

- [Getting Started](https://github.com/Nano-Collective/json-up/blob/main/docs/getting-started.md)
- [API Reference](https://github.com/Nano-Collective/json-up/blob/main/docs/api-reference.md)
- [Error Handling](https://github.com/Nano-Collective/json-up/blob/main/docs/error-handling.md)
- [Examples](https://github.com/Nano-Collective/json-up/blob/main/docs/examples.md)

## Community

We're a small community-led team building local and privacy-first AI solutions under the [Nano Collective](https://nanocollective.org/) and would love your help! Whether you're interested in contributing code, documentation, or just being part of our community, there are several ways to get involved.

**If you want to contribute to the code:**

- Read our detailed [CONTRIBUTING.md](https://github.com/Nano-Collective/json-up/blob/main/CONTRIBUTING.md) guide for information on development setup, coding standards, and how to submit your changes.

**If you want to be part of our community or help with other aspects like design or marketing:**

- Join our Discord server to connect with other users, ask questions, share ideas, and get help: [Join our Discord server](https://discord.gg/ktPDV6rekE)
- Head to our GitHub issues or discussions to open and join current conversations with others in the community.]]></content:encoded>
            <category>Packages</category>
        </item>
        <item>
            <title><![CDATA[Schedule mode is coming to Nanocoder... Run project background tasks on a cron schedule 🚀]]></title>
            <link>https://nanocollective.org/blog/schedule-mode-is-coming-to-nanocoder-run-project-background-tasks-on-a-cron-schedule-33</link>
            <guid isPermaLink="false">https://nanocollective.org/blog/schedule-mode-is-coming-to-nanocoder-run-project-background-tasks-on-a-cron-schedule-33</guid>
            <pubDate>Wed, 18 Feb 2026 21:52:59 GMT</pubDate>
            <description><![CDATA[We've been working on a new feature for [Nanocoder](https://github.com/Nano-Collective/nanocoder) which we're quite excited about - a built-in cron scheduler that lets you set up recurring AI tasks.

## The idea

Write a markdown prompt describing a task, attach a cron expression, and let the agent handle the rest on a schedule. Dependency updates every Monday morning. Issue triage every hour. Daily lint runs with auto-fix. Whatever repetitive dev work you keep putting off.

How it'll work]]></description>
            <content:encoded><![CDATA[We've been working on a new feature for [Nanocoder](https://github.com/Nano-Collective/nanocoder) which we're quite excited about - a built-in cron scheduler that lets you set up recurring AI tasks.

## The idea

Write a markdown prompt describing a task, attach a cron expression, and let the agent handle the rest on a schedule. Dependency updates every Monday morning. Issue triage every hour. Daily lint runs with auto-fix. Whatever repetitive dev work you keep putting off.

How it'll work (at the moment, open to feedback)
- `/schedule create deps-update` — scaffolds a schedule file and the AI helps you write the prompt
- `/schedule add "0 9 * * MON" deps-update.md` - registers it with a cron expression
- `/schedule start` - enters scheduler mode where jobs fire automatically

Jobs run sequentially with a queue, all tool calls (including bash) are auto-accepted, and there's full run history via /schedule logs.

Schedule files are just markdown with optional YAML frontmatter - easy to version control and share across a team.

Anyway, it's coming soon and would love any thoughts :D

## What's next

This runs while Nanocoder is open - it's not a system daemon (yet). We're exploring missed-run detection on startup and potentially a detached background mode.]]></content:encoded>
            <category>Nanocoder</category>
        </item>
        <item>
            <title><![CDATA[Nanocoder 1.22.0: New UI, Interactive File Explorer, Task Management, and More]]></title>
            <link>https://nanocollective.org/blog/nanocoder-1220-new-ui-interactive-file-explorer-task-management-and-more-31</link>
            <guid isPermaLink="false">https://nanocollective.org/blog/nanocoder-1220-new-ui-interactive-file-explorer-task-management-and-more-31</guid>
            <pubDate>Sat, 07 Feb 2026 22:24:07 GMT</pubDate>
            <description><![CDATA[We're excited to announce Nanocoder 1.22.0! It's packed with powerful new features that make working with your codebase more intuitive and productive than ever.

## What's New in This Release

### Interactive File Explorer with `/explorer`

Navigating complex codebases just got a lot easier. The new `/explorer` command brings an interactive file browser directly into your terminal with:

- **Tree view navigation** for intuitive directory exploration
- **File preview** with syntax highli]]></description>
            <content:encoded><![CDATA[We're excited to announce Nanocoder 1.22.0! It's packed with powerful new features that make working with your codebase more intuitive and productive than ever.

## What's New in This Release

### Interactive File Explorer with `/explorer`

Navigating complex codebases just got a lot easier. The new `/explorer` command brings an interactive file browser directly into your terminal with:

- **Tree view navigation** for intuitive directory exploration
- **File preview** with syntax highlighting to quickly understand code context
- **Multi-file selection** to work with multiple files at once
- **Search mode** for finding exactly what you need
- **VS Code integration** so selected files can be opened in your editor

Try it out by typing `/explorer` in any chat session - it's perfect for getting oriented in unfamiliar projects or surfacing the right files for your AI assistant to review.

### Task Management for Complex Workflows

We've finally bought a new task management system. The model can automatically create tasks or you can use the `/tasks` command to break down and track progress on multi-step work:

- **Create, list, update, and delete tasks** with dedicated tools
- **Persistent storage** in `.nanocoder/tasks.json` so tasks survive between sessions
- **Automatic cleanup** when you restart the CLI or run `/clear`
- Perfect for feature development, bug fixes, and refactoring projects

Now you can have clear, shared visibility into what needs to be done and what's already complete. It was a long time coming.

### Enhanced Provider Support

We've added several improvements for working with different AI providers:

**Native Gemini & Anthropic Support**: Configure the `sdkProvider` option to use Google's native Gemini integration as well as Anthropic's. This allows more flexibility for provider use.

**Custom Headers**: Need to authenticate through tunnels like Cloudflare? Provider configurations now support custom headers for advanced networking scenarios.

**Kimi Code Provider**: A new provider template has been added to the configuration wizard for easy setup.

### Settings Interface and New Themes

Customization is easier than ever. The `/settings` command opens an interactive menu to configure your UI theme and shape preferences without editing config files manually. We've also added beautiful new themes and updated styling for improved visual clarity and consistency.

### Improved UI

We've given the new version of Nanocoder a fresh look. Assistant/User messages now look cleaner than ever. We've also given token information per message to help you keep track of what context going in and coming out of the model over and above the `/usage` command.

## Under the Hood

This release includes important improvements to developer experience:

- **Git tool refactoring** for better consistency, error handling, and user feedback
- **Line truncation** in file operations to prevent excessive output on narrow terminals
- **Enhanced security** with multiple vulnerability fixes

## Bug Fixes

- Fixed `/usage` command crashes when context data is unavailable
- Improved string replace error handling for edge cases
- Security audit issues resolved

## Get Started

Update Nanocoder to version 1.22.0 today and experience the improvements - we hope you love it :)

```bash
npm install -g nanocoder
# or with your preferred package manager
```]]></content:encoded>
            <category>Nanocoder</category>
        </item>
        <item>
            <title><![CDATA[Nanocoder 1.21.0: Enhanced Configuration, Smarter Tool Handling, and Better Reliability]]></title>
            <link>https://nanocollective.org/blog/nanocoder-1210-enhanced-configuration-smarter-tool-handling-and-better-reliability-27</link>
            <guid isPermaLink="false">https://nanocollective.org/blog/nanocoder-1210-enhanced-configuration-smarter-tool-handling-and-better-reliability-27</guid>
            <pubDate>Wed, 21 Jan 2026 22:08:09 GMT</pubDate>
            <description><![CDATA[We're excited to announce Nanocoder 1.21.0, a release focused on making your local AI coding experience more powerful, flexible, and reliable. This version introduces hierarchical configuration management, intelligent tool support detection, and a collection of quality-of-life improvements that developers have been asking for.

## Hierarchical Configuration Loading

One of the most requested features is now here: **project-level configuration overrides**. Nanocoder 1.21.0 introduces hierarch]]></description>
            <content:encoded><![CDATA[We're excited to announce Nanocoder 1.21.0, a release focused on making your local AI coding experience more powerful, flexible, and reliable. This version introduces hierarchical configuration management, intelligent tool support detection, and a collection of quality-of-life improvements that developers have been asking for.

## Hierarchical Configuration Loading

One of the most requested features is now here: **project-level configuration overrides**. Nanocoder 1.21.0 introduces hierarchical configuration loading for both provider configs and MCP servers. This means you can now:

- Set up global provider configurations in `~/.config/nanocoder/`
- Override them with project-specific settings in your local `.nanocoder/` directory
- Have Claude Code's object-style MCP configuration format automatically recognized and supported

This makes it incredibly easy to manage different configurations for different projects without manually switching settings between tasks.

## Automatic Tool Support Detection

Models vary widely in their capabilities - some don't support function calling at all. We've added **automatic tool support error detection with intelligent retry logic**. When Nanocoder detects that a model doesn't support tool calling, it automatically retries the request without tools, allowing the conversation to continue seamlessly. No more errors or failed requests for models with limited capabilities.

## Message Compression at Scale

Working with large conversations can be challenging. The new `/compact` command lets you compress your conversation context with the `--restore` flag to recover original messages if needed. Combined with auto-compact functionality and consistent token counting, managing lengthy sessions is now more straightforward.

## New Commands & Configuration Options

We've added several useful commands and options:

- **`/quit`** - A new alternative to exit Nanocoder (complementing the existing exit methods)
- **`/nanocoder-shape`** - Select your preferred branding font styles for custom commands
- **`--version` and `--help`** - Quick reference CLI options at your fingertips
- **`alwaysAllow` configuration option** - Mark trusted MCP server tools to skip confirmation prompts

## Under the Hood

Beyond these headline features, we've fixed critical issues:

- **Bash tool now properly cleans up processes** - Previously, background processes could persist after command completion
- **Improved bash security** - Resolved shell command security scanning alerts from environment values
- **Better error handling** - Enhanced tool error detection and retry mechanisms
- **Better UX** - Improved `/mcp` and `/help` command styling with better colors and formatting

## Try It Out

We love getting developer feedback so try it out today!

If you have feedback or encounter any issues, please reach out through our [GitHub issues page](https://github.com/Nano-Collective/nanocoder/issues) or join us on Discord.

Happy coding! 🙌
]]></content:encoded>
            <category>Nanocoder</category>
        </item>
        <item>
            <title><![CDATA[Nanocoder 1.20.0: Fresh Year, Fresh Features]]></title>
            <link>https://nanocollective.org/blog/nanocoder-1200-fresh-year-fresh-features-26</link>
            <guid isPermaLink="false">https://nanocollective.org/blog/nanocoder-1200-fresh-year-fresh-features-26</guid>
            <pubDate>Fri, 09 Jan 2026 13:29:28 GMT</pubDate>
            <description><![CDATA[Happy New Year! 🥳

We're excited to kick off 2026 with Nanocoder 1.20.0, a release packed with powerful features, comprehensive testing, and significant quality improvements. Whether you're using Nanocoder for rapid development, code exploration, or complex multi-file refactoring, this release brings meaningful enhancements across every layer of the platform.

It was a labour of love over the holidays from our contributors!

Before we go into the update though, we all want to thank you fo]]></description>
            <content:encoded><![CDATA[Happy New Year! 🥳

We're excited to kick off 2026 with Nanocoder 1.20.0, a release packed with powerful features, comprehensive testing, and significant quality improvements. Whether you're using Nanocoder for rapid development, code exploration, or complex multi-file refactoring, this release brings meaningful enhancements across every layer of the platform.

It was a labour of love over the holidays from our contributors!

Before we go into the update though, we all want to thank you for an incredible 2025 - Nanocoder was launched just 5-months ago and its quality, community and vision has only grown.

Heading into 2026 we've crossed 1K stars and get over 4,000 downloads a month. We have an incredible core team now and so many amazing contributors putting in work to make Nanocoder great.

Our mission has alway been to build community-led solutions that focus on privacy and local-first AI. We're only going to continue on that mission this year. Thanks to everyone!

## What's New in 1.20.0

### Visual Enhancements

We've introduced **Catpuccin themes** with full gradient color support, giving you four beautiful options: Latte, Frappe, Macchiato, and Mocha. Whether you prefer light or dark aesthetics, these carefully curated themes make extended coding sessions more personalised to you. Plus, we've added **stylized title boxes** with powerline-style shapes and real-time preview support in custom commands, giving your terminal output a polished, professional appearance. We believe your CLI experience should be yours and made to be beautiful.

### VS Code Plugin Improvements

Right-clicking code in VS Code? Now you can directly ask Nanocoder about it through the **context menu integration**. This tighter editor integration means fewer context switches and faster feedback loops during development.

### Expanded Language Support

Language Server Protocol (LSP) support has been significantly expanded with **automatic project detection** for Deno, GraphQL, Docker, and Markdown language servers. The setup wizard now features **auto-fetch models**, allowing providers to automatically discover available models during configuration. This means less manual setup and better out-of-the-box experiences.

### Git Workflow Tools

We've added comprehensive **git workflow integration**, including:
- Smart commit message generation
- PR template creation
- Branch naming suggestions
- Enhanced status reporting

These tools leverage AI to streamline your git workflows, reducing friction in your development process.

### Performance & Reliability

- **90%+ code coverage** across components, hooks, tools, and utilities with comprehensive unit and integration tests
- **File content caching** reduces tool confirmation delays and improves responsiveness
- **Real-time bash output progress** with live updates during command execution
- **Inline word-level highlighting** in string_replace diff display for clearer visualization of changes

### Security Improvements

- Path boundary validation prevents directory traversal attacks
- Granular debug logging with structured Pino logger for better error tracking
- Enhanced error handling with AI SDK error types
- Switched to crypto.randomUUID and crypto.randomBytes for secure ID generation

### Infrastructure

- **Devcontainer support** for streamlined development environments
- **Automated PR checks workflow** with format, type, lint, and test validation
- Improved tool caller prompts and descriptions for better model interaction
- Refactored core components for maintainability

## Under the Hood

This release includes significant architectural improvements: the App component has been refactored into focused modules (useAppState, useAppInitialization, useChatHandler, useToolHandler), file operations have been switched to async operations for better performance, and multiple files have been renamed to kebab-case for consistency.

Code exploration tools are now smarter with better prompts and a new `list_directories` tool. Ignored file patterns are now centralized for consistency, and tool formatters provide improved indentation for readability.

## Get Started Today

Nanocoder 1.20.0 is available now and ready for use. Whether you're upgrading for the new themes, git integration, or the comprehensive testing improvements, you'll notice immediate benefits in your workflow.

[Download the latest version](https://github.com/Nano-Collective/nanocoder) and explore what makes Nanocoder the developer's choice for AI-assisted coding.

Thank you to our incredible community of contributors who made this release possible. Your feedback, bug reports, and pull requests drive our continuous improvement. If you have thoughts or run into any issues, reach out via our [GitHub issues](https://github.com/Nano-Collective/nanocoder/issues) or join us on [Discord](https://discord.gg/nanocoder).]]></content:encoded>
            <category>Nanocoder</category>
        </item>
        <item>
            <title><![CDATA[Nanocoder 1.19.0: Non-Interactive Mode, Conversation Checkpointing, and Enterprise-Grade Logging]]></title>
            <link>https://nanocollective.org/blog/nanocoder-1190-non-interactive-mode-conversation-checkpointing-and-enterprise-grade-logging-22</link>
            <guid isPermaLink="false">https://nanocollective.org/blog/nanocoder-1190-non-interactive-mode-conversation-checkpointing-and-enterprise-grade-logging-22</guid>
            <pubDate>Tue, 16 Dec 2025 19:52:46 GMT</pubDate>
            <description><![CDATA[Nanocoder 1.19.0 is here, and it's another big one! Packed with powerful new features designed to make your AI-powered coding workflow more flexible, reliable, and production-ready.

## What's New in 1.19.0

### Non-Interactive Mode for CI/CD Pipelines

We've included non-interactive mode. You can now run Nanocoder directly in CI/CD pipelines and automation scripts by passing commands via CLI arguments. Nanocoder executes your request and exits automatically. No interactive prompts, no wai]]></description>
            <content:encoded><![CDATA[Nanocoder 1.19.0 is here, and it's another big one! Packed with powerful new features designed to make your AI-powered coding workflow more flexible, reliable, and production-ready.

## What's New in 1.19.0

### Non-Interactive Mode for CI/CD Pipelines

We've included non-interactive mode. You can now run Nanocoder directly in CI/CD pipelines and automation scripts by passing commands via CLI arguments. Nanocoder executes your request and exits automatically. No interactive prompts, no waiting for user input.

This opens up exciting possibilities:
- Integrate Nanocoder into your GitHub Actions, GitLab CI, or Jenkins workflows
- Automate code reviews, refactoring, and documentation tasks
- Use Nanocoder as part of your build pipeline for intelligent code generation

```bash
# Example: Run Nanocoder non-interactively
nanocoder "Summarise what the README.md file says"
```

### Conversation Checkpointing System

We've implemented an interactive conversation checkpointing system that lets you save and restore your chat sessions across different workdays and projects. Never lose context again. Your conversations are now persistent, allowing you to:

- Save complex multi-step conversations for later reference
- Resume work exactly where you left off
- Maintain project-specific context across sessions
- Build knowledge bases from your interactions

### Enterprise-Grade Pino Logging

For teams running Nanocoder in production environments, we've integrated a comprehensive structured logging system powered by Pino. This new logging infrastructure provides:

- Structured logging for easier parsing and analysis
- Request tracking across tool executions
- Performance monitoring to identify bottlenecks
- Configurable log levels (debug, info, warn, error)
- Perfect for debugging issues in containerized environments

### Tooling Improvements

- **Faster Code Quality Tooling**: We've switched from Prettier and ESLint to Biome, delivering faster formatting and linting without sacrificing code quality standards.
- **New Provider Templates**: Added support for Poe.com and Mistral AI in the configuration wizard, expanding your LLM provider options.
- **Cleaner Dependencies**: Removed the `ink-titled-box` dependency and replaced it with a custom, lightweight implementation—reducing bloat and bugs while maintaining visual quality.

### Under the Hood

- Updated Ollama model contexts for improved compatibility
- Added `--force` flag to `/init` command for regenerating AGENTS.md without user prompts
- Fixed critical security vulnerabilities flagged by pnpm audits
- Fixed README table of contents anchors for proper GitHub fork navigation
- Refactored GitHub Actions workflows to reduce duplication and improve maintainability

## Get Started Today

Have questions or feedback? Drop us a line on [Discord](https://discord.gg/nanocoder) or open an issue on [GitHub](https://github.com/Nano-Collective/nanocoder). We'd love to hear how you're using Nanocoder!

Thank you for being part of the Nanocoder community. 🙌
]]></content:encoded>
            <category>Nanocoder</category>
        </item>
        <item>
            <title><![CDATA[Nanocoder 1.18.0: Multi-Step Tool Calls, Better Debugging, and a Smarter Model Database]]></title>
            <link>https://nanocollective.org/blog/nanocoder-1180-multi-step-tool-calls-better-debugging-and-a-smarter-model-database-19</link>
            <guid isPermaLink="false">https://nanocollective.org/blog/nanocoder-1180-multi-step-tool-calls-better-debugging-and-a-smarter-model-database-19</guid>
            <pubDate>Mon, 08 Dec 2025 21:40:14 GMT</pubDate>
            <description><![CDATA[We're excited to announce Nanocoder 1.18.0, a release packed with improvements to how the AI agent handles tool calls, new debugging capabilities, and a completely revamped model discovery experience.

## AI SDK v6 Beta: Multi-Step Tool Calls Are Here

The headline feature of this release is the upgrade to AI SDK v6 beta. This brings significant improvements to model and tool calling performance, but more importantly, it introduces **multi-step tool calls support**.

What does this mean in]]></description>
            <content:encoded><![CDATA[We're excited to announce Nanocoder 1.18.0, a release packed with improvements to how the AI agent handles tool calls, new debugging capabilities, and a completely revamped model discovery experience.

## AI SDK v6 Beta: Multi-Step Tool Calls Are Here

The headline feature of this release is the upgrade to AI SDK v6 beta. This brings significant improvements to model and tool calling performance, but more importantly, it introduces **multi-step tool calls support**.

What does this mean in practice? Previously, when Nanocoder needed to perform multiple operations, each tool call was handled sequentially with the model reasoning between each step. With multi-step tool calls, the agent can now chain operations more efficiently, leading to faster task completion and more coherent workflows.

This upgrade was contributed by @DenizOkcu, who also fixed locale-related test failures that were affecting some users.

## New `/debugging` Command

Sometimes you need to see exactly what's happening under the hood. The new `/debugging` command toggles detailed tool call information, showing you the raw data being passed between the model and tools. This is invaluable when:

- Troubleshooting unexpected behavior
- Understanding how the model is interpreting your requests
- Contributing to Nanocoder development

Thanks again to @DenizOkcu for this addition.

## From `/recommendations`, to `/model-database`

We've replaced the `/recommendations` command with a more powerful `/model-database` command. Instead of static recommendations, you now have access to a searchable database of models with up-to-date information about capabilities, context sizes, and more.

This change makes it significantly easier for us to maintain accurate model information and for you to find the right model for your use case.

## Cleaner Status Display

LSP and MCP server connection status is now displayed directly in the Status component. This provides much cleaner visibility into your connected services without cluttering the main UI with verbose connection messages. Thanks to @Avtrkrb for this improvement.

## GitHub Issue Templates

We've added structured issue templates for bug reports and feature requests. If you encounter problems or have ideas, these templates will help you provide the information we need to act quickly.

## A Note on Streaming

We've temporarily removed streaming output. The AI SDK v6 upgrade introduced some layout and flickering issues that affected the user experience. We're working on bringing streaming back in a future release once these issues are resolved.

## Breaking Changes

None! This is a drop-in upgrade.

## Get It Now

```bash
# npm
npm update -g @nanocollective/nanocoder

# homebrew
brew upgrade nanocoder

# nix
nix flake update
```

## Community

Nanocoder is built by a growing community of contributors. Special thanks to @DenizOkcu and @Avtrkrb for their contributions to this release.

Have feedback or found a bug? [Open an issue](https://github.com/nano-collective/nanocoder/issues)

---

_Nanocoder is a local-first AI coding agent that works with any OpenAI-compatible API. It's open source, privacy-respecting, and designed for developers who want AI assistance without vendor lock-in._]]></content:encoded>
            <category>Nanocoder</category>
        </item>
        <item>
            <title><![CDATA[Nanocoder v1.16.x to v1.17.0]]></title>
            <link>https://nanocollective.org/blog/nanocoder-v116x-to-v1170-17</link>
            <guid isPermaLink="false">https://nanocollective.org/blog/nanocoder-v116x-to-v1170-17</guid>
            <pubDate>Wed, 26 Nov 2025 23:07:22 GMT</pubDate>
            <description><![CDATA[We're excited to share an overview of the recent Nanocoder releases, from the v1.16.x series through to v1.17.0. These releases represent huge community contributions, bug fixes, and significant new capabilities that make Nanocoder more powerful and user-friendly than ever.

## v1.16.0: The /usage Command and Better Tool Handling

The v1.16.0 release introduced a much-requested feature: the `/usage` command. Thanks to @spinualexandru, users can now visually monitor their model's context usag]]></description>
            <content:encoded><![CDATA[We're excited to share an overview of the recent Nanocoder releases, from the v1.16.x series through to v1.17.0. These releases represent huge community contributions, bug fixes, and significant new capabilities that make Nanocoder more powerful and user-friendly than ever.

## v1.16.0: The /usage Command and Better Tool Handling

The v1.16.0 release introduced a much-requested feature: the `/usage` command. Thanks to @spinualexandru, users can now visually monitor their model's context usage directly in the CLI. This closes issue #12 and gives developers better insight into how much of their context window is being consumed during sessions.

This release also expanded the model recommendations database with new models and fixed an important issue where models would occasionally request permission to call non-existent tools. The system now properly errors and loops back to allow the model to self-correct.

## v1.16.1: Quick Patch for Installation

A swift patch release addressed installation failures caused by a postinstall hook that referenced a missing scripts directory. The fix moved models.dev data fetching from installation time to first use, making installations smoother across all platforms.

## v1.16.2: Stability and Output Management

This release focused on stability improvements:

- **Empty response handling**: Models returning empty responses after tool execution are now automatically reprompted
- **False positive prevention**: HTML tags in responses no longer trigger incorrect tool call detection errors
- **Output limits**: `search_files` results capped at 10 (down from 50) and `execute_bash` output truncated to 2,000 characters to prevent context overflow

## v1.16.3: A Complete Tool System Overhaul

Version 1.16.3 brought substantial changes to how Nanocoder handles file operations and searching:

**Out with the old:**

- Removed `read_many_files` (rarely used over `read_file`)
- Removed `search_files` (models found it confusing)

**In with the new:**

- **`find_files`**: Pattern-based file and directory discovery
- **`search_file_contents`**: Content-aware searching with metadata
- **Revised `read_file`**: Now reveals progressive information, returning metadata first with optional line ranges for specific content

These changes were designed specifically to help smaller models like Qwen 3 Coder 30B perform better by reducing context rot through more purposeful tool calls.

Additional fixes included:

- Update checker rendering (previously appeared perpetually stuck)
- Proper XDG config path handling (`~/.config/nanocoder/`)
- Numerous edge-case fixes in the markdown parser
- Removal of message display limits (the optimised queue can now handle full session history)

## v1.16.4: Platform Improvements and New Theme

A community-driven release with several quality-of-life improvements:

- **Config/data separation**: Clear distinction between configuration and application data directories (thanks @bowmanjd)
- **Smart update detection**: The update checker now auto-detects your installation method (npm, Homebrew, Nix) and provides appropriate update instructions (thanks @fabriziosalmi)
- **Dracula theme**: A new dark theme option for Dracula enthusiasts
- **VSCode Terminal fixes**: Creating new lines and paste detection now work correctly in VSCode's integrated terminal
- **Better autocomplete**: Command autocomplete now selects the top suggestion rather than waiting for a single remaining option

## v1.16.5: Cleanup and Nix Fixes

The final patch in the 1.16.x series focused on refinement:

- `/init` command no longer generates `agents.config.json` (aligning with new configuration patterns)
- Code refactoring to reduce duplication (thanks @JimStenstrom)
- Critical fix for broken Nix installation (thanks @Thomashighbaugh)

---

## v1.17.0: VS Code Extension and MCP Overhaul

The v1.17.0 release marks a significant milestone for Nanocoder with two major features:

### VS Code Extension v1

Nanocoder now ships with an official VS Code extension featuring:

- **Live code diffs**: See changes as they're proposed
- **Diagnostics integration**: LSP support for language-aware feedback
- **Seamless workflow**: Work with Nanocoder directly in your editor

This is version 1 of the extension with plenty of room for expansion and improvement based on community feedback.

### MCP Configuration Redesign

Thanks to the extensive work by @Avtrkrb, the MCP (Model Context Protocol) system received a complete overhaul:

- **Tab-based UI**: Redesigned MCP configuration screens using a clean tab layout
- **Remote MCP support**: Added support for remote MCP servers alongside stdio servers
- **GitHub Remote MCP**: New template with Auth header configuration
- **Expanded templates**: Additional MCP server templates for common use cases
- **Better sequencing**: Configuration wizard now follows a logical stdio -> remote flow

### Additional Improvements

- Global configuration now properly loads when local/project configuration is missing
- Preflight checks and test improvements
- Documentation updates for new MCP fields

---

## Thank You to Our Contributors

These releases wouldn't be possible without our amazing community. Special thanks to:

- **@spinualexandru** - /usage command implementation
- **@bowmanjd** - Config/data directory separation and XDG path fixes
- **@fabriziosalmi** - Smart update detection
- **@Avtrkrb** - MCP configuration overhaul
- **@JimStenstrom** - Code refactoring
- **@Thomashighbaugh** - Nix installation fixes

## Get Started

Update to the latest version using your preferred method:

```bash
# npm
npm update -g @nanocollective/nanocoder

# Homebrew
brew update && brew upgrade nanocoder

# Nix
nix profile upgrade nanocoder
```

If you encounter any problems or have feedback, please drop an issue on [GitHub](https://github.com/Nano-Collective/nanocoder/issues) or message us through Discord.

Thank you for using Nanocoder!
]]></content:encoded>
            <category>Nanocoder</category>
        </item>
        <item>
            <title><![CDATA[Next Up: LSP Support & IDE Plugin Implementation]]></title>
            <link>https://nanocollective.org/blog/next-up-lsp-support-ide-plugin-implementation-13</link>
            <guid isPermaLink="false">https://nanocollective.org/blog/next-up-lsp-support-ide-plugin-implementation-13</guid>
            <pubDate>Wed, 12 Nov 2025 22:15:33 GMT</pubDate>
            <description><![CDATA[
**Status:** In Development Queue
**Issue:** [#52](https://github.com/Nano-Collective/nanocoder/issues/52)
**Priority:** High

## Overview

We're excited to announce that LSP (Language Server Protocol) support and IDE plugin implementation is up next in Nanocoder's development roadmap. This feature represents a significant leap forward in bringing deep code intelligence capabilities to our AI coding assistant.

## What's Coming?

The upcoming implementation will add comprehensive LSP ]]></description>
            <content:encoded><![CDATA[
**Status:** In Development Queue
**Issue:** [#52](https://github.com/Nano-Collective/nanocoder/issues/52)
**Priority:** High

## Overview

We're excited to announce that LSP (Language Server Protocol) support and IDE plugin implementation is up next in Nanocoder's development roadmap. This feature represents a significant leap forward in bringing deep code intelligence capabilities to our AI coding assistant.

## What's Coming?

The upcoming implementation will add comprehensive LSP support to Nanocoder, enabling it to leverage the same powerful code intelligence tools that modern IDEs use. This means the AI will have access to:

- **Real-time diagnostics** - Syntax errors, type checking, and linting issues
- **Code navigation** - Jump to definitions, find references, and symbol lookups
- **Type information** - Hover details and signature help
- **Intelligent refactoring** - Code actions, renaming, and completions
- **Workspace awareness** - Full understanding of project structure and dependencies

## Two-Part Approach

### Part 1: LSP Client in Nanocoder

The core enhancement will embed an LSP client directly into Nanocoder, automatically discovering and connecting to language servers in your development environment. This zero-configuration approach means:

- Auto-detection of installed language servers
- Support for multiple simultaneous connections (TypeScript, Python, Rust, etc.)
- 10 new AI tools powered by LSP data
- Graceful fallbacks when LSP isn't available

### Part 2: VS Code Extension

A dedicated VS Code extension will provide seamless IDE integration with:

- **Chat panel sidebar** - Interact with Nanocoder without leaving your editor
- **Editor integration** - Right-click context menus for quick AI assistance
- **File watching** - Bidirectional synchronization between VS Code and Nanocoder
- **Workspace awareness** - Full context of your project structure

## Design Philosophy

True to Nanocoder's ethos, LSP support will follow a zero-configuration philosophy. It should "just work" out of the box, with:

- Automatic detection of language servers
- No required configuration files or setup steps
- Intelligent fallbacks when LSP data isn't available
- Optional advanced configuration for power users

## Implementation Timeline

The development will follow a six-phase approach:

1. **LSP Foundation** - Core client implementation and auto-discovery
2. **Tools Integration** - New LSP-powered AI tools
3. **Context Enhancement** - Intelligent context building with LSP data
4. **Extension Server** - Communication bridge for IDE integration
5. **VS Code Extension** - Full-featured IDE plugin
6. **Testing & Polish** - Comprehensive testing and refinements

## Why This Matters

LSP support will fundamentally transform Nanocoder's capabilities. Instead of relying solely on file reading and text analysis, the AI will have access to the same rich semantic understanding that language servers provide to your IDE. This means:

- **More accurate suggestions** based on actual type information
- **Fewer errors** by leveraging real-time diagnostics
- **Better refactoring** with awareness of all symbol references
- **Faster development** with IDE-integrated workflows

## Get Involved

This is an ambitious feature that will take Nanocoder to the next level. We're starting development soon and would love community input:

- Share your thoughts on the [GitHub issue](https://github.com/Nano-Collective/nanocoder/issues/52)
- Suggest additional LSP capabilities you'd like to see
- Help us prioritize which language servers to support first

Stay tuned for updates as we begin implementation!

---

*Nanocoder is a local-first AI coding assistant built with React and Ink.js, supporting multiple LLM providers including Ollama, OpenRouter, and OpenAI-compatible APIs.*
]]></content:encoded>
            <category>Nanocoder</category>
        </item>
        <item>
            <title><![CDATA[Introducing AI-Powered HTML to Markdown Conversion in get-md]]></title>
            <link>https://nanocollective.org/blog/introducing-ai-powered-html-to-markdown-conversion-in-get-md-12</link>
            <guid isPermaLink="false">https://nanocollective.org/blog/introducing-ai-powered-html-to-markdown-conversion-in-get-md-12</guid>
            <pubDate>Wed, 12 Nov 2025 22:03:18 GMT</pubDate>
            <description><![CDATA[We're excited to announce an upcoming feature for `@nanocollective/get-md`: optional AI-powered HTML to Markdown conversion using locally-run language models. This enhancement will give developers the choice between lightning-fast conversion and AI-enhanced quality, all while maintaining complete offline functionality.

## The Challenge

While our current Turndown-based conversion is fast and reliable (sub-100ms for most pages), complex HTML structures like nested tables, intricate layouts, ]]></description>
            <content:encoded><![CDATA[We're excited to announce an upcoming feature for `@nanocollective/get-md`: optional AI-powered HTML to Markdown conversion using locally-run language models. This enhancement will give developers the choice between lightning-fast conversion and AI-enhanced quality, all while maintaining complete offline functionality.

## The Challenge

While our current Turndown-based conversion is fast and reliable (sub-100ms for most pages), complex HTML structures like nested tables, intricate layouts, and heavily formatted content can sometimes result in less-than-perfect Markdown output. We wanted to offer a solution that could handle these edge cases better without forcing all users to adopt a slower, more resource-intensive approach.

## The Solution: Optional Local LLM

We're adding support for ReaderLM-v2, a 1.5 billion parameter language model specifically designed for HTML-to-Markdown conversion. The key aspects of our approach:

- **Completely optional**: Turndown remains the default. Users opt-in via a simple flag.
- **Runs locally**: No API keys, no cloud services, no internet required (after model download).
- **Full control**: Developers using get-md in their applications can let their end-users decide whether to enable AI conversion.
- **Smart fallback**: If the LLM fails for any reason, the system automatically falls back to Turndown.

## How It Works

### For CLI Users

```bash
# Check if the model is available
getmd --model-info

# Download the AI model (986MB, one-time)
getmd --download-model

# Convert using AI
getmd https://example.com --use-llm
```

The CLI provides interactive prompts and progress bars to guide users through the model download process.

### For SDK/Package Users

```typescript
import {
  convertToMarkdown,
  checkLLMModel,
  downloadLLMModel
} from '@nanocollective/get-md';

// Check if model is available
const status = await checkLLMModel();

if (!status.available) {
  // Download with progress callbacks
  await downloadLLMModel({
    onProgress: (downloaded, total, percentage) => {
      console.log(`Downloading: ${percentage.toFixed(1)}%`);
    }
  });
}

// Convert with AI
const result = await convertToMarkdown('https://example.com', {
  useLLM: true,
  onLLMEvent: (event) => {
    // React to conversion events
    if (event.type === 'conversion-complete') {
      console.log(`Done in ${event.duration}ms`);
    }
  }
});
```

## Perfect for Application Developers

One of the most powerful aspects of this feature is how it works for developers building applications with get-md. Whether you're creating an Obsidian plugin, VS Code extension, Electron app, or web service, you can give your users the choice to enable AI conversion.

### Example: Obsidian Plugin

```typescript
// In your plugin settings
new Setting(containerEl)
  .setName('AI-powered conversion')
  .setDesc('Better quality, slower (986MB model)')
  .addToggle(toggle => toggle
    .setValue(this.settings.useAI)
    .onChange(async (value) => {
      this.settings.useAI = value;
      await this.saveSettings();
    }));

// Add a download button if model isn't installed
if (!modelAvailable) {
  new Setting(containerEl)
    .addButton(button => button
      .setButtonText('Download AI Model')
      .onClick(async () => {
        await downloadLLMModel({
          onProgress: (_, __, percentage) => {
            button.setButtonText(`Downloading... ${percentage}%`);
          }
        });
      }));
}
```

Your users get a seamless experience with progress feedback, and the model downloads directly to their machine—no server-side infrastructure needed on your end.

## Performance Trade-offs

We believe in transparency, so here's what you need to know:

| Metric | Turndown (Default) | LLM (Optional) |
|--------|-------------------|----------------|
| **Speed** | <100ms | 5-10 seconds |
| **RAM** | ~100MB | 2-4GB |
| **Disk** | 0MB | 986MB (model) |
| **Quality** | Good | Excellent |
| **Complex layouts** | Good | Excellent |
| **Consistency** | 100% deterministic | ~95% consistent |

### When to Use Each

- **Use Turndown** (default) for:
  - Batch processing thousands of pages
  - CI/CD pipelines
  - Real-time conversion
  - Low-resource environments
  - Simple blog posts and articles

- **Use LLM** (opt-in) for:
  - High-quality single conversions
  - Complex documentation sites
  - Pages with intricate tables and nested structures
  - When visual fidelity matters most

## Technical Architecture

The LLM integration is designed to enhance, not replace, the existing pipeline:

1. **Content extraction** (Readability) - removes ads, nav, etc.
2. **HTML cleaning** - removes scripts, normalizes structure
3. **Structure enhancement** - fixes heading hierarchy
4. **Conversion** - Either Turndown OR LLM (user's choice)
5. **Post-processing** - adds frontmatter, calculates stats

By running the LLM on pre-cleaned HTML, we get better results while reducing token usage and processing time.

## Callback System for Rich UIs

We've designed a comprehensive callback system so developers can build rich user interfaces around the conversion process:

```typescript
await convertToMarkdown(url, {
  useLLM: true,
  onLLMEvent: (event) => {
    switch (event.type) {
      case 'model-loading':
        showSpinner('Loading AI model...');
        break;
      case 'download-progress':
        updateProgressBar(event.percentage);
        break;
      case 'conversion-complete':
        hideSpinner();
        showSuccess(`Converted in ${event.duration}ms`);
        break;
      case 'fallback-start':
        showWarning('Using fallback method');
        break;
    }
  }
});
```

This means whether you're building a CLI tool, desktop app, or web service, you can provide users with real-time feedback on what's happening.

## Why ReaderLM-v2?

We chose ReaderLM-v2 for several reasons:

- **Purpose-built**: Specifically designed for HTML-to-Markdown conversion
- **Efficient**: 1.5B parameters with GGUF quantization (Q4_K_M) runs on consumer hardware
- **Multilingual**: Supports 29 languages out of the box
- **Proven**: Outperforms much larger models on conversion tasks
- **Open source**: Available on Hugging Face, MIT-compatible licensing

The Q4_K_M quantized version strikes the perfect balance—high quality at 986MB, running comfortably on systems with 8GB+ RAM.

## Open Questions & Future Enhancements

We're considering several enhancements for future versions:

- **Hybrid mode**: Use LLM only for complex sections (tables, nested structures), Turndown for simple content
- **GPU acceleration**: Faster inference on compatible hardware
- **Caching layer**: Store conversions to avoid re-processing identical content
- **Alternative models**: Support for other models based on user preference
- **Smart detection**: Automatically choose method based on HTML complexity

We'd love to hear your feedback on which features would be most valuable to you.

## No Breaking Changes

This is a purely additive feature. All existing code continues to work exactly as before. The default behavior remains unchanged—fast, deterministic Turndown conversion. LLM conversion is opt-in only.

## Try It Out

The implementation is currently in planning stages. We're working through the architecture and will be rolling this out in phases:

- **Phase 1**: Core infrastructure and LLM integration
- **Phase 2**: CLI and SDK integration with callbacks
- **Phase 3**: Enhanced features (model management, config files)
- **Phase 4**: Comprehensive testing and documentation

Follow our [GitHub repository](https://github.com/nano-collective/get-md) for updates, or check out [Issue #3](https://github.com/Nano-Collective/get-md/issues/3) to join the discussion.

## We Want Your Feedback

This feature is being designed with the community in mind. We'd love to hear:

- What use cases would benefit most from AI-powered conversion?
- What should the user experience look like in your application?
- Are there specific models or configurations you'd like to see supported?
- What concerns do you have about the implementation?

Join the conversation on GitHub or reach out to us directly. Let's build something amazing together.

---

*get-md is an open-source HTML to Markdown converter optimized for LLM consumption, built by the Nano Collective. Fast, reliable, and now with optional AI enhancement.*
]]></content:encoded>
            <category>Packages</category>
        </item>
        <item>
            <title><![CDATA[Nanocoder Recent Releases Overview – v1.15.0 -> v1.16.0]]></title>
            <link>https://nanocollective.org/blog/nanocoder-recent-releases-overview-v1150-v1160-11</link>
            <guid isPermaLink="false">https://nanocollective.org/blog/nanocoder-recent-releases-overview-v1150-v1160-11</guid>
            <pubDate>Mon, 10 Nov 2025 16:28:02 GMT</pubDate>
            <description><![CDATA[# Nanocoder Recent Updates: v1.15.0 to v1.16.4

Here's what's been happening with Nanocoder lately! We've been busy shipping some pretty cool stuff from v1.15.0 to v1.16.4 - including a complete backend overhaul, new features for tracking context usage and tagging files, tool system refinements for better context management, configuration directory improvements, and a bunch of important fixes to make everything run smoother. Let's dive in.

## v1.16.4 - Configuration, Updates & Input Handlin]]></description>
            <content:encoded><![CDATA[# Nanocoder Recent Updates: v1.15.0 to v1.16.4

Here's what's been happening with Nanocoder lately! We've been busy shipping some pretty cool stuff from v1.15.0 to v1.16.4 - including a complete backend overhaul, new features for tracking context usage and tagging files, tool system refinements for better context management, configuration directory improvements, and a bunch of important fixes to make everything run smoother. Let's dive in.

## v1.16.4 - Configuration, Updates & Input Handling

**Release Focus**: Configuration/data separation, intelligent update detection, and improved terminal compatibility

### New Features

**New**: Dracula theme added to the theme selector

### Major Changes

**Big**: Decoupled configuration vs data directories to introduce clear separation between configuration and application data directories (contributed by @bowmanjd) 🎉

- Config now lives in platform-specific config directories (e.g., `~/.config/nanocoder/`)
- Data stored separately in platform-specific data directories
- Optional `NANOCODER_DATA_DIR` environment variable for custom data location
- Automatic migration from old paths with user feedback

**Big**: Update checker now intelligently detects how you installed Nanocoder and provides appropriate update instructions (contributed by @fabriziosalmi)

- Auto-detects installation method (npm, Homebrew, Nix, etc.)
- Shows correct update commands based on detection
- Provides manual update steps if auto-detection fails

### Fixes

**Fix**: Command auto-complete now auto-completes to the top suggestion regardless of how many matches exist (previously only worked with single match)

**Fix**: Improved paste detection to create placeholders for any pasted content (removed 80-char minimum), fixed consecutive paste placeholder sizing, and improved paste chunking for VSCode and other terminals

**Fix**: Creating new lines in VSCode Terminal was broken - now fully functional

**Fix**: History navigation issues resolved

---

## v1.16.3 - Tool System Overhaul

**Release Focus**: Context management optimization and tool refinement for smaller models

### Major Changes

**Big**: Complete tool system redesign to better manage context and improve accuracy for smaller models like Qwen 3 Coder 30B

### New Tools

**New**: `find_files` tool - Model provides a pattern and gets back matching files and directory paths

**New**: `search_file_contents` tool - Model provides a pattern and gets back matching content with metadata for further use

### Tool Improvements

**Enhanced**: `read_file` tool now reveals progressive information about files

- Called alone: Returns just file metadata
- With line number ranges: Returns specific content
- Reduces unnecessary context loading

### Removed Tools

**Removed**: `read_many_files` tool - Rarely used by models over `read_file` with little benefit

**Removed**: `search_files` tool - Models often found it confusing when trying to differentiate finding files vs searching content

### Enhancements

**Enhancement**: Removed message display limit - You can now see entire session history (message queue is well optimized)

**Enhancement**: Many edge-case fixes in the markdown parser for assistant messages - Outputs are far cleaner now

### Fixes

**Fix**: Update checker used old rendering method so appeared broken and always checking for an update - Now fully functional

**Fix**: Config files now correctly use `~/.config/nanocoder/` (or platform equivalents) instead of `~/.local/share/nanocoder/`, restoring proper XDG semantic separation between configuration and data (contributed by @bowmanjd)

---

## v1.16.2 - Output Management & Parsing Improvements

**Release Focus**: Enhanced tool output handling and model response reliability

### Fixes

**Fix**: Models returning empty responses after tool execution now automatically reprompted

**Fix**: HTML tags in responses no longer trigger false tool call detection errors

### Enhancements

**Enhancement**: `search_files` tool limited to 10 results (reduced from 50) to prevent large outputs

**Enhancement**: `execute_bash` output truncated to 2,000 chars (reduced from 4,000) and returns plain string

**Enhancement**: Model context limit tests updated to match actual implementation

---

## v1.16.1 - Installation Fix

**Release Focus**: Resolving installation issues for better package reliability

### Fixes

**Fix**: Removed postinstall hook that caused installation failures when scripts directory was missing from published package

**Change**: Models.dev data now fetched on first use instead of during installation

---

## v1.16.0 - Usage Tracking & Stability

**Release Focus**: Context usage visibility and tool calling reliability

### New Features

**New**: `/usage` command for visualizing model context usage (contributed by @spinualexandru, closes #12) 🎉

### Enhancements

**Enhancement**: Added new models to recommendations database

### Fixes

**Fix**: Resolved issue where model requested permission for non-existent tools - Now errors and loops back to the model to self-correct

---

## v1.15.1 - Compatibility Fixes

**Release Focus**: Cross-platform compatibility and edge case handling

### Fixes

**Fix**: Ollama tool calls without IDs no longer cause empty responses - IDs now auto-generated when missing

**Fix**: Homebrew installer now working correctly

**Fix**: Node version requirement updated to 20+

---

## v1.15.0 - Major Architecture Update

**Release Focus**: Backend modernization and enhanced user experience

### Major Changes

**Big**: Switched backend architecture from LangGraph to AI SDK - A better fit for Nanocoder (contributed by @DenizOkcu)

### New Features

**New**: File tagging with `@` symbol - Fuzzy search and select files to include in messages directly

**New**: Real-time message streaming to see agent respond in realtime - Toggle stream mode on/off via the `/streaming` command

**New**: Homebrew installation option

### Enhancements

**Enhancement**: Command auto-complete now uses fuzzy search for better discoverability

**Enhancement**: Replaced custom table renderer with the more robust `cli-table3` library

**Enhancement**: Refined XML/JSON parsing flow for non-native tool calls

---

## Summary

These releases demonstrate Nanocoder's commitment to:

**Stability**: Critical fixes for installation, tool calling, terminal compatibility, and cross-platform support

**Performance**: Architectural improvements with AI SDK migration, optimized context management, and refined tool system

**User Experience**: New features like context usage tracking, file tagging, real-time streaming, intelligent update detection, and improved configuration management

**Community**: Multiple contributor acknowledgments (@spinualexandru, @DenizOkcu, @bowmanjd, @fabriziosalmi) and continued focus on user feedback

The journey from v1.15.0 to v1.16.4 represents both major architectural milestones (AI SDK migration, tool system overhaul, config/data separation) and incremental refinements based on real-world usage and community feedback. Special focus on making Nanocoder work better with smaller models through intelligent context management shows our commitment to accessibility and performance across all hardware configurations.

---

Want to get involved? Drop a comment or an issue on the relevant repo 😎
]]></content:encoded>
            <category>Nanocoder</category>
        </item>
        <item>
            <title><![CDATA[New Feature Development: Homebrew Installation Support 🍺]]></title>
            <link>https://nanocollective.org/blog/new-feature-development-homebrew-installation-support-10</link>
            <guid isPermaLink="false">https://nanocollective.org/blog/new-feature-development-homebrew-installation-support-10</guid>
            <pubDate>Tue, 04 Nov 2025 16:16:52 GMT</pubDate>
            <description><![CDATA[One of the next things up for Nanocoder is official Homebrew support. This has been our _longest_ open issue so we thought we'd better get to it 😉

### What this means for you:
Installing and updating Nanocoder will be as simple as:

```bash
brew install Nano-Collective/nanocoder/nanocoder
```
### Key benefits:
- Streamlined installation for macOS and Linux users
- Automatic updates with each new release
- Standardized package management that integrates with your existing workflow
-]]></description>
            <content:encoded><![CDATA[One of the next things up for Nanocoder is official Homebrew support. This has been our _longest_ open issue so we thought we'd better get to it 😉

### What this means for you:
Installing and updating Nanocoder will be as simple as:

```bash
brew install Nano-Collective/nanocoder/nanocoder
```
### Key benefits:
- Streamlined installation for macOS and Linux users
- Automatic updates with each new release
- Standardized package management that integrates with your existing workflow
- One-command upgrades to always stay current

### What we're building:

- Native Homebrew formula in our main repository
- Automated formula updates with every release
- Comprehensive documentation and troubleshooting guides
- Rigorous testing across multiple macOS versions

This addition makes Nanocoder even more accessible while maintaining our commitment to local-first, privacy-respecting AI tools.

Stay tuned for the official release.]]></content:encoded>
            <category>Nanocoder</category>
        </item>
        <item>
            <title><![CDATA[New Feature Development: Adding @ File Syntax to Nanocoder]]></title>
            <link>https://nanocollective.org/blog/new-feature-development-adding-file-syntax-to-nanocoder-9</link>
            <guid isPermaLink="false">https://nanocollective.org/blog/new-feature-development-adding-file-syntax-to-nanocoder-9</guid>
            <pubDate>Fri, 31 Oct 2025 15:06:19 GMT</pubDate>
            <description><![CDATA[### Update: Adding @ File Syntax to Nanocoder

Next up for Nanocoder, we'll be working on `@` syntax for file references, based on the feature request in issue #5.

### What's changing:
You'll be able to include files directly in your messages using `@` syntax. Instead of asking Nanocoder to read files separately, you can reference them inline:

`"Fix the TypeScript errors in @src/utils/validation.ts and update @types/user.ts accordingly"`

### Planned features:

- File inclusion with]]></description>
            <content:encoded><![CDATA[### Update: Adding @ File Syntax to Nanocoder

Next up for Nanocoder, we'll be working on `@` syntax for file references, based on the feature request in issue #5.

### What's changing:
You'll be able to include files directly in your messages using `@` syntax. Instead of asking Nanocoder to read files separately, you can reference them inline:

`"Fix the TypeScript errors in @src/utils/validation.ts and update @types/user.ts accordingly"`

### Planned features:

- File inclusion with @filename.ts
- Line range support with @filename.ts:10-20
- Multiple file references in one message
- Tab completion for file paths
- Glob pattern support

This should streamline your workflow by reducing the steps needed to work with multiple files.

We'll share another discussion once released!]]></content:encoded>
            <category>Nanocoder</category>
        </item>
        <item>
            <title><![CDATA[New Package: LLM Scorer / Profiler / Bench based on system]]></title>
            <link>https://nanocollective.org/blog/new-package-llm-scorer-profiler-bench-based-on-system-4</link>
            <guid isPermaLink="false">https://nanocollective.org/blog/new-package-llm-scorer-profiler-bench-based-on-system-4</guid>
            <pubDate>Wed, 29 Oct 2025 13:37:08 GMT</pubDate>
            <description><![CDATA[# New Package: LLM Scorer / Profiler / Bench based on system

In [Nanocoder](https://github.com/Nano-Collective/nanocoder) we offer LLM recommendations via a [`/recommendations` command](https://github.com/Nano-Collective/nanocoder/issues/43). This has proven useful for users to see how well a particular LLM will run on a system.

We're taking this idea and building an isolated package that takes in a list of models and system information, returning a series of metrics for each model and an ]]></description>
            <content:encoded><![CDATA[# New Package: LLM Scorer / Profiler / Bench based on system

In [Nanocoder](https://github.com/Nano-Collective/nanocoder) we offer LLM recommendations via a [`/recommendations` command](https://github.com/Nano-Collective/nanocoder/issues/43). This has proven useful for users to see how well a particular LLM will run on a system.

We're taking this idea and building an isolated package that takes in a list of models and system information, returning a series of metrics for each model and an overall performance score.

## How it might work...

This package produces metrics for each model, based on...

- User's system information (memory, CPU, GPU, TPU, Swap, Arch, ...)
- LLM's model card information (arch, quant, ...)
- LLM's skills (vision, tools, ...)

... then rate how well the LLM operates on the user's system.

Primarily this package will be used to rate local LLMs on the runtime's system, but we also see merits in scoring skills and allowing you to override system information too. We also need to consider when the list of models features entries that are cloud-side, meaning system information doesn't weigh in nearly as much.

Usage may look something like this:

```typescript
const result = llmScorer({
    models: [
        {
            model: 'meta-llama/Llama-3.1-8B',
            quantization: ...,
            arch: 'llama',
            // Many other optional parameters for this model...
        },
        {
            model: 'meta-llama/Llama-3.2-1B',
            quantization: ...,
            arch: 'llama',
        },
    ],
    system: {
        // optionally override default behaviour of pulling system information
        memoryGB: 24,
        // ...
    }
})
```

And the results will look something like this, ordered by best rated model first:

```
[
    {
        model: "meta-llama/Llama-3.1-8B",
        score: 0.8,
        metrics: {
            ...
        }
    },
    {
        ...
    },
]
```

# What Next

We're in the process of building an initial solution and plan to release this on a public repository to gather feedback from our community and evolve it.

We recognise that there's many angles and opinions to consider for this, so we welcome contributors and our community to help form how this package works.

We'll announce the alpha version of this package once it is prepared and released.

# Community
We're a small community-led team building local and privacy-first AI solutions under the [Nano Collective](https://nanocollective.org/) and would love your help! Whether you're interested in contributing code, documentation, or just being part of our community, there are several ways to get involved.

**If you want to contribute to this new concept:**

- Drop comments and thoughts below, or [join our Discord server](https://discord.gg/ktPDV6rekE)

**If you want to be part of our community or help with other aspects like code, design or marketing:**

- Join our Discord server to connect with other users, ask questions, share ideas, and get help: [Join our Discord server](https://discord.gg/ktPDV6rekE)

- Head to our GitHub issues or discussions to open and join current conversations with others in the community.
]]></content:encoded>
            <category>Packages</category>
        </item>
        <item>
            <title><![CDATA[New Package: Lightweight HTML to Markdown utility for LLM use]]></title>
            <link>https://nanocollective.org/blog/new-package-lightweight-html-to-markdown-utility-for-llm-use-3</link>
            <guid isPermaLink="false">https://nanocollective.org/blog/new-package-lightweight-html-to-markdown-utility-for-llm-use-3</guid>
            <pubDate>Wed, 29 Oct 2025 12:27:10 GMT</pubDate>
            <description><![CDATA[# New Package: Lightweight HTML to Markdown utility for LLM use

Our members saw the sheer speed of [Markitdown](https://github.com/microsoft/markitdown) in the Python space, so we begun to wonder what lessons we could bring to the Node JS space, with LLM use in mind.

---

**Now Released**: new LLM oriented utility for converting HTML to Markdown - [@nanocollective/get-md](https://github.com/Nano-Collective/get-md)

---

For speed, markdown is often a preferable format for LLMs to dig]]></description>
            <content:encoded><![CDATA[# New Package: Lightweight HTML to Markdown utility for LLM use

Our members saw the sheer speed of [Markitdown](https://github.com/microsoft/markitdown) in the Python space, so we begun to wonder what lessons we could bring to the Node JS space, with LLM use in mind.

---

**Now Released**: new LLM oriented utility for converting HTML to Markdown - [@nanocollective/get-md](https://github.com/Nano-Collective/get-md)

---

For speed, markdown is often a preferable format for LLMs to digest content. So when you want to process a webpage, converting it to markdown helps strip out anything but the relevant content an LLM needs to perform a job.

We produced a binary version of Markitdown that can be bundled into a Node JS package (see [@mote-software/markitdown](https://github.com/Mote-Software/markitdown)) but the cold-boot performance hit is much worse than we hoped. We'll explore options to speed this up, but ultimately using binaries  - especially Python based - will always have a degraded performance.

So with a focus on speed and quality, this new Node JS package can take in HTML and produce a markdown format optimised for LLM use within moments (~100ms), and save you magnitudes of compute.

> [!TIP]
> We'd like to bring more input formats too, so you could convert PDF, DOCX, audio, and other file formats... just like [Markitdown](https://github.com/microsoft/markitdown) does. [We welcome contributors](https://github.com/Nano-Collective/get-md/blob/main/CONTRIBUTING.md) to help our community with this mission.

# Community
We're a small community-led team building local and privacy-first AI solutions under the [Nano Collective](https://nanocollective.org/) and would love your help! Whether you're interested in contributing code, documentation, or just being part of our community, there are several ways to get involved.

**If you want to contribute to the code:**

- Read our detailed [CONTRIBUTING.md](https://github.com/Nano-Collective/get-md/blob/main/CONTRIBUTING.md) guide for information on development setup, coding standards, and how to submit your changes.

**If you want to be part of our community or help with other aspects like design or marketing:**

- Join our Discord server to connect with other users, ask questions, share ideas, and get help: [Join our Discord server](https://discord.gg/ktPDV6rekE)

- Head to our GitHub issues or discussions to open and join current conversations with others in the community.]]></content:encoded>
            <category>Packages</category>
        </item>
    </channel>
</rss>